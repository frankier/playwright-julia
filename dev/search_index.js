var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/wglmakie/#WGLMakie.jl","page":"WGLMakie.jl","text":"An interactive plot, screenshotted and traced. The hardest of the four examples, and the one that justifies the artifact API: a WebGL canvas is exactly the case where \"the assertion failed\" tells you nothing and a screenshot tells you everything.\n\n$ julia --project=examples examples/wglmakie_jl.jl\n$ PLAYWRIGHT_JL_ENGINE=firefox julia --project=examples examples/wglmakie_jl.jl","title":"WGLMakie.jl"},{"category":"section","location":"examples/wglmakie/#Chromium-renders.-Firefox-has-no-WebGL-at-all.","page":"WGLMakie.jl","text":"Stating this plainly, because it decides what the example can claim.\n\nChromium renders the figure in full, with no launch flags. Playwright's Chromium ships SwiftShader — a CPU rasteriser — and selects it when there is no GPU, so WebGL works headless, with no GPU and no X display. Both --use-gl=swiftshader and the --enable-unsafe-swiftshader incantation that circulates for this problem produce a byte-identical screenshot to passing nothing at all. The renderer reports itself as:\n\nANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver)\n\nHeadless Firefox has no WebGL context whatsoever. Not a slow one, not a broken one — getContext('webgl2') and getContext('webgl') both return null. Four escalating attempts changed nothing: webgl.force-enabled and friends, a Mesa software-GL environment (LIBGL_ALWAYS_SOFTWARE, GALLIUM_DRIVER=llvmpipe), and both together. The console explains itself:\n\nFailed to create WebGL context: WebGL creation failed:\n* Exhausted GL driver options. (FEATURE_FAILURE_WEBGL_EXHAUSTED_DRIVERS)\n\nFirefox looked for a driver it would accept and found none, which is why pointing it at a software rasteriser changes nothing.\n\nSo the rendered-pixel assertion runs on Chromium only. Firefox is still run, and still asserted on, structurally: the canvas is created at the requested size, getContext returns nothing, and the page raises no errors. WGLMakie's no-WebGL fallback is a designed path rather than a crash, which makes \"no page errors\" a real assertion on both engines rather than a tautology on one.","title":"Chromium renders. Firefox has no WebGL at all."},{"category":"section","location":"examples/wglmakie/#Waiting-for-pixels","page":"WGLMakie.jl","text":"There is no DOM event for \"WebGL has painted\". The canvas element exists long before anything is drawn on it, so wait_for_selector returns far too early — the first version of the probe did exactly that and screenshotted Bonito's loading spinner.\n\nWhat is observable is the screenshot. So the wait is on the pixels: count the distinct colours in a screenshot and retry until the plot appears. The figure is deliberately high-variance — three series in red, blue and green — so a blank result cannot be explained away as a flat plot, and the three states are an order of magnitude apart:\n\nPage state distinct colours\nBonito still loading 123\nFirefox's no-WebGL fallback 241\nfully rendered ~1690\n\nThe threshold is 500, with an order of magnitude of headroom on both sides.\n\nThe budget is 90 seconds, and it needs to be. A cold first render measured 57.9 seconds — Bonito serving its bundle, the browser compiling the WGLMakie JavaScript, and SwiftShader compiling shaders on the CPU, all at once — against 1.8 seconds warm. CI is the cold case every single time. This is retry_until applied to rendering rather than to server start-up, and it is the second place in these examples where a fixed sleep would be either a flake or a minute thrown away. See Waiting.","title":"Waiting for pixels"},{"category":"section","location":"examples/wglmakie/#The-artifacts","page":"WGLMakie.jl","text":"The run is wrapped in with_tracing and ends with a screenshot, both written to examples/output/ (gitignored).\n\nThat is not decoration. When a WebGL page fails in CI, the assertion message is false — the trace's filmstrip and the screenshot are the only things that can tell you whether the canvas was blank, the plot was wrong, or the page never loaded. Open the trace with:\n\n$ npx playwright@1.61.1 show-trace examples/output/wglmakie-chromium.zip\n\nSee Artifacts.","title":"The artifacts"},{"category":"section","location":"examples/wglmakie/#The-source","page":"WGLMakie.jl","text":"Read from examples/wglmakie_jl.jl at build time:\n\nusing Markdown, Playwright\nsrc = read(joinpath(pkgdir(Playwright), \"examples\", \"wglmakie_jl.jl\"), String)\nMarkdown.parse(\"```julia\\n\" * src * \"```\")","title":"The source"},{"category":"section","location":"getting-started/#Getting-started","page":"Getting started","text":"","title":"Getting started"},{"category":"section","location":"getting-started/#Install","page":"Getting started","text":"using Pkg\nPkg.add(url = \"https://github.com/frankier/playwright-julia\")\n\nPlaywright.jl is not registered yet, so it installs from the repository.","title":"Install"},{"category":"section","location":"getting-started/#The-first-run-downloads-things","page":"Getting started","text":"There is no browser in this package, and no browser in its artifacts. On first use it fetches two things:\n\nthe upstream playwright-core npm package — the same driver the Node, Python and .NET clients drive — at a pinned version;\na pinned Node.js binary from nodejs.org to run it.\n\nThey are assembled into a Julia scratch space keyed by both versions. Browsers themselves are downloaded by the driver into the standard Playwright cache (~/.cache/ms-playwright), shared with any other Playwright installation on the machine.\n\nThis means the very first launch on a clean machine can take a couple of minutes. Nothing is wrong; it is downloading a browser.\n\nusing Playwright\ninstall()                            # driver + Chromium + Firefox, ahead of time\ninstall(; browsers = [\"chromium\"])   # or just the one you need\n\nPlaywright.browsers_path reports where browsers will be looked for, which is the first thing to check when a launch cannot find one.","title":"The first run downloads things"},{"category":"section","location":"getting-started/#A-first-test","page":"Getting started","text":"using Test\nusing Playwright\n\n@testset \"the homepage\" begin\n    playwright() do pw\n        browser = launch(pw.chromium; headless = true)\n        try\n            page = new_page(browser)\n            goto(page, \"https://example.com\")\n\n            expect(page; to_have_title = \"Example Domain\")\n            expect(locator(page, \"h1\"); to_have_text = \"Example Domain\")\n        finally\n            close(browser)\n        end\n    end\nend\n\nThree things in that shape are deliberate and worth copying:\n\nplaywright takes a block. The driver subprocess is shut down on the way out however the block ends. There is no playwright() that returns a handle for you to remember to close.\nclose(browser) is in a finally. A browser left running outlives the test process on some platforms.\nThe assertions are expect, not @test title(page) == …. expect retries until the condition holds; the @test form reads once and fails if the page was a few milliseconds slow. This is the single most important habit in the package — see Assertions.\n\nFor a test that also collects a screenshot and a trace when it fails, use the with_page fixture instead of the try/finally above; see Artifacts.","title":"A first test"},{"category":"section","location":"getting-started/#Choosing-an-engine","page":"Getting started","text":"pw.chromium and pw.firefox are the two supported engines. WebKit exists in the protocol but is not tested by this package.\n\nplaywright() do pw\n    for bt in (pw.chromium, pw.firefox)\n        browser = launch(bt; headless = true)\n        try\n            # ...the same test, on both engines\n        finally\n            close(browser)\n        end\n    end\nend\n\nbrowser_name tells you which one you are on, for the rare case where the engines genuinely differ and a test has to say so out loud.","title":"Choosing an engine"},{"category":"section","location":"getting-started/#Browsers-in-CI","page":"Getting started","text":"install needs Playwright.jl to be loadable, which is exactly what a project carrying it as a test dependency does not have outside Pkg.test(). bin/install.jl is the way in — it runs standalone, activating the checkout itself if the active environment cannot load the package:\n\n$ julia bin/install.jl                 # driver + Chromium + Firefox\n$ julia bin/install.jl chromium        # just the one you need\n\nAn unknown browser name is rejected before anything downloads.\n\nSet PLAYWRIGHT_BROWSERS_PATH to put browsers somewhere you control, which is far easier to cache and restore than a path in the home directory. Installing and launching both read it — the driver subprocess inherits Julia's environment — so set it once and the two agree:\n\n$ export PLAYWRIGHT_BROWSERS_PATH=\"$PWD/.playwright\"\n$ julia bin/install.jl chromium","title":"Browsers in CI"},{"category":"section","location":"getting-started/#A-GitHub-Actions-job","page":"Getting started","text":"This is close to what this repository runs against itself:\n\njobs:\n  browser-tests:\n    runs-on: ubuntu-latest\n    env:\n      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/.playwright\n    steps:\n      - uses: actions/checkout@v4\n      - uses: julia-actions/setup-julia@v2\n      - uses: julia-actions/cache@v2\n\n      - uses: actions/cache@v4\n        id: browsers\n        with:\n          path: ${{ github.workspace }}/.playwright\n          # Key on the Playwright version: that is what decides which browser\n          # build is needed.\n          key: playwright-${{ runner.os }}-1.61.1\n\n      - name: Install browsers\n        if: steps.browsers.outputs.cache-hit != 'true'\n        run: julia --project=. bin/install.jl\n\n      - uses: julia-actions/julia-runtest@v1\n\nTwo things that are easy to get wrong:\n\nCheck that the cache actually hits. A key that never matches is a cache that never saves you anything, and it fails silently — the job just stays slow. Read the log of a second run and confirm the install step was skipped.\nThe driver bundle is separate from the browsers. It lives in a Julia scratch space, which julia-actions/cache@v2 already covers by default. Caching it yourself as well means two actions writing the same path.","title":"A GitHub Actions job"},{"category":"section","location":"getting-started/#Headless,-and-the-display","page":"Getting started","text":"Everything here passes headless = true, and on a CI runner there is no alternative. Headless Chromium in particular ships a software rasteriser, so even WebGL content renders with no GPU and no X display. Headless Firefox has no WebGL at all — see WGLMakie.jl for what that does and does not cover.","title":"Headless, and the display"},{"category":"section","location":"getting-started/#Where-to-go-next","page":"Getting started","text":"Locators — finding elements\nWaiting — and why it is not sleeping\nAssertions — expect and retry_until\nEvents — popups, console output, page errors\nArtifacts — evidence from a failing test\nErrors and timeouts — what each failure means","title":"Where to go next"},{"category":"section","location":"guide/events/#Events","page":"Events","text":"Some things a page does are not requests you make — a popup opening, a console message, an uncaught error. Those arrive as events.\n\nThe one thing to understand about them is a race: you must be subscribed before the action that triggers the event. An event fired synchronously by a click is gone before a subscription made afterwards exists. That is why the primary form takes a block.\n\npopup = expect_event(ctx, :page) do\n    click(locator(page, \"#open-popup\"))\nend\n\nexpect_event subscribes, runs the block, and then waits for the event. Nothing the block does can be missed.","title":"Events"},{"category":"section","location":"guide/events/#What-you-can-subscribe-to","page":"Events","text":"Owner Event Payload\nPage :close, :crash the Page\nPage :frameattached, :framedetached Frame\nBrowserContext :page Page — this is how you catch a popup\nBrowserContext :close the BrowserContext\nBrowserContext :console ConsoleMessage\nBrowserContext :pageerror PageError\n\nAnything else raises ArgumentError. Network events (:request, :response, …), :dialog and :download are deferred rather than designed away: their payload types exist in the generated layer but have no accessors yet, and handing one back would look like support without being it.","title":"What you can subscribe to"},{"category":"section","location":"guide/events/#Filtering","page":"Events","text":"predicate picks the event you meant out of several of the same kind. The first payload it accepts is returned; rejected payloads are consumed, not requeued.\n\nmsg = expect_event(ctx, :console; predicate = m -> m.text == \"ready\") do\n    click(locator(page, \"#go\"))\nend","title":"Filtering"},{"category":"section","location":"guide/events/#Waiting-for-something-already-in-flight","page":"Events","text":"When the trigger is not yours — a page closing itself after a countdown that is already running — there is nothing to put in a block, and wait_for_event is the form to use:\n\nwait_for_event(page, :close; timeout = 10_000)\n\nUse it only in that case. When you trigger the event, expect_event exists precisely because subscribing afterwards is a race.","title":"Waiting for something already in flight"},{"category":"section","location":"guide/events/#Collecting-several","page":"Events","text":"with_events gives the block a live EventStream instead of waiting for one payload:\n\nerrors = with_events(ctx, :pageerror) do stream\n    click(locator(page, \"#break-everything\"))\n    retry_until(() -> length(stream) >= 2; timeout = 5_000)\n    pending_events(stream)\nend\n\nThe buffer is unbounded and attached before the block runs, so nothing that happens inside is dropped while you are not looking. It is released when the block ends, however it ends.\n\nTwo ways to read it, and the difference matters:\n\nlength peeks — it counts without consuming, so it is the thing to poll on;\npending_events drains — everything buffered right now, and a second call returns only what arrived since;\nnext_event blocks for one at a time, with its own timeout and predicate.\n\nPolling pending_events in a loop waiting for a count to be reached does not work, because each call empties the buffer it is counting. Poll length.","title":"Collecting several"},{"category":"section","location":"guide/events/#Postmortem,-not-events","page":"Events","text":"For \"what went wrong on this page\", you usually do not want events at all. console_messages and page_errors read a buffer the driver keeps per page, with no subscription and no block:\n\nfor err in page_errors(page)\n    @warn \"page error\" err.message err.stack\nend\n\nThese are safe to call from a finally while a more important error is in flight — they return an empty vector rather than raising once the page has closed. See Artifacts.\n\nUse events when you need to catch the moment; use these when you need to explain a failure afterwards.","title":"Postmortem, not events"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"Every exported name, grouped by what it is for rather than alphabetically — alphabetical order puts click next to clear_page_errors and separates start_tracing from stop_tracing, which helps nobody.\n\ncheckdocs = :exports is on, so this page is complete by construction: an export missing from it fails the build.","title":"API reference"},{"category":"section","location":"api/#Sessions-and-lifecycle","page":"API reference","text":"","title":"Sessions and lifecycle"},{"category":"section","location":"api/#Objects","page":"API reference","text":"","title":"Objects"},{"category":"section","location":"api/#Navigation-and-capture","page":"API reference","text":"","title":"Navigation and capture"},{"category":"section","location":"api/#Locator-construction","page":"API reference","text":"","title":"Locator construction"},{"category":"section","location":"api/#Reading-an-element","page":"API reference","text":"","title":"Reading an element"},{"category":"section","location":"api/#Acting-on-an-element","page":"API reference","text":"","title":"Acting on an element"},{"category":"section","location":"api/#Frame-navigation","page":"API reference","text":"","title":"Frame navigation"},{"category":"section","location":"api/#Evaluating-JavaScript","page":"API reference","text":"","title":"Evaluating JavaScript"},{"category":"section","location":"api/#Assertion-functions","page":"API reference","text":"","title":"Assertion functions"},{"category":"section","location":"api/#Waiting-functions","page":"API reference","text":"","title":"Waiting functions"},{"category":"section","location":"api/#Artifact-capture","page":"API reference","text":"","title":"Artifact capture"},{"category":"section","location":"api/#Diagnostic-readers","page":"API reference","text":"","title":"Diagnostic readers"},{"category":"section","location":"api/#Timeout-settings","page":"API reference","text":"","title":"Timeout settings"},{"category":"section","location":"api/#Error-types","page":"API reference","text":"","title":"Error types"},{"category":"section","location":"api/#Base-extensions","page":"API reference","text":"These extend their Base counterparts rather than taking a new name, so they work unqualified — count(loc), not Playwright.count(loc) — but they do not appear in names(Playwright) and so are outside checkdocs' reach. They are public surface all the same.","title":"Base extensions"},{"category":"section","location":"api/#Internals","page":"API reference","text":"Not exported, and not part of the supported surface. They are here because the docstrings above refer to them.","title":"Internals"},{"category":"module","location":"api/#Playwright","page":"API reference","text":"Playwright\n\nDrive real browsers (Chromium & Firefox) from Julia through the official Playwright automation engine. See the README for a quick start:\n\nusing Playwright\n\nplaywright() do pw\n    browser = launch(pw.chromium; headless=true)\n    page = new_page(browser)\n    goto(page, \"https://example.com\")\n    println(title(page))\n    close(browser)\nend\n\n\n\n\n\n","title":"Playwright"},{"category":"function","location":"api/#Playwright.playwright","page":"API reference","text":"playwright(f) -> result of `f`\n\nStart the Playwright driver, call f(pw) with a PlaywrightAPI (fields chromium, firefox), and guarantee driver shutdown when the block exits — normally or by exception. Installs the driver on first use.\n\nplaywright() do pw\n    browser = launch(pw.chromium)\n    # ...\n    close(browser)\nend\n\n\n\n\n\n","title":"Playwright.playwright"},{"category":"function","location":"api/#Playwright.launch","page":"API reference","text":"launch(browser_type::BrowserType; headless=true, timeout=180_000, kwargs...) -> Browser\n\nLaunch a browser instance of browser_type (e.g. pw.chromium), waiting up to timeout ms for it to start.\n\ntimeout here is the one timeout in the package that does not follow the set_default_timeout! cascade, and deliberately so: it bounds browser process startup, not an action on a page, and at launch time there is no page or context in existence to inherit a setting from. It keeps its own 180 s default because a cold browser start is far slower than any action.\n\nEvery option below is optional and is omitted from the protocol message entirely when left unset, so the driver's own defaults apply:\n\nOption Meaning\nargs Extra browser command-line arguments\nchromium_sandbox Enable Chromium's sandbox (off by default in containers)\nenv Environment for the browser process, as a Dict\nfirefox_user_prefs Firefox about:config preferences, as a Dict\nexecutable_path Run this browser binary instead of the bundled one\nchannel Branded channel, e.g. \"chrome\" or \"msedge\"\nslow_mo Delay each operation by this many ms, for debugging\nproxy Proxy settings, e.g. Dict(\"server\" => \"http://…\")\ndownloads_path Where to put downloads\n\nexecutable_path and channel are what make CHROME_BIN-style provisioning work on a machine that already has a browser installed.\n\nnote: Options for the other engine are ignored, not errors\nargs and chromium_sandbox mean nothing to Firefox, and firefox_user_prefs means nothing to Chromium — but passing them anyway is harmless. The driver ignores what does not apply to the engine it is launching.Probed against the 1.61.1 driver on both engines: Firefox launched with args and chromium_sandbox, Chromium launched with firefox_user_prefs, and one shared option set launched both — every combination started and rendered a page. So one option set can be shared across engines without partitioning it per engine, which is why this package ships no launch_options(engine; …) filter: there is nothing for it to prevent.\n\n# Fine on both engines, despite half of it applying to neither.\nopts = (; args = [\"--disable-dev-shm-usage\"], chromium_sandbox = false,\n          firefox_user_prefs = Dict(\"dom.disable_beforeunload\" => true))\nfor bt in (pw.chromium, pw.firefox)\n    browser = launch(bt; headless = true, opts...)\n    # ...\nend\n\nlaunch(pw.chromium; headless=true, chromium_sandbox=false,\n       args=[\"--disable-dev-shm-usage\"])\n\n\n\n\n\n","title":"Playwright.launch"},{"category":"function","location":"api/#Playwright.install","page":"API reference","text":"install(; browsers=[\"chromium\", \"firefox\"])\n\nDownload the Playwright driver bundle (if needed) and install browsers into the standard Playwright browser cache via the driver's own installer. Safe to call repeatedly — both steps are no-ops when already done.\n\nbrowsers names engines the driver understands: \"chromium\", \"firefox\", \"webkit\". launch calls this for you the first time an engine turns out to be missing, so an explicit install is for the case where you want the download to happen now — in a CI step of its own, say, rather than inside the first test.\n\ninstall()                          # both engines\ninstall(; browsers = [\"chromium\"]) # just one\n\nSet PLAYWRIGHT_BROWSERS_PATH to put the browsers somewhere cacheable rather than in the default per-user cache. From outside a Julia session, the same thing is julia bin/install.jl, which works from a bare checkout — the case a package carrying Playwright.jl as a test dependency hits in CI.\n\n\n\n\n\n","title":"Playwright.install"},{"category":"function","location":"api/#Playwright.new_context","page":"API reference","text":"new_context(browser::Browser; kwargs...) -> BrowserContext\n\nOpen a fresh browser context — an isolated profile with its own cookies, storage and permissions, and the unit of isolation between tests. Contexts are cheap; a new browser is not.\n\nOptions (all optional, omitted from the wire when unset): viewport (a Dict or NamedTuple of width/height), user_agent, locale, timezone_id, color_scheme, device_scale_factor, is_mobile, has_touch, offline, permissions, base_url, extra_http_headers (a Dict), ignore_https_errors, java_script_enabled, record_video.\n\nctx = new_context(browser; viewport=(width=1280, height=720))\npage = new_page(ctx)\nclose(ctx)\n\nrecord_video takes a dir and an optional size, and recording is context-scoped because that is what the protocol offers — there is no per-page switch. See video for reading the result, and note that the file is not complete until the page or context closes:\n\nctx = new_context(browser; record_video = (dir = \"artifacts/video\",))\n\n\n\n\n\n","title":"Playwright.new_context"},{"category":"function","location":"api/#Playwright.new_page","page":"API reference","text":"new_page(browser::Browser) -> Page\nnew_page(context::BrowserContext) -> Page\n\nOpen a new page. Given a Browser, a fresh context is created to hold it and is closed again by close(page) — so a per-test page leaks nothing. Given a BrowserContext, the page joins that context and its lifetime is yours.\n\npage = new_page(browser)     # its own context, cleaned up with the page\ngoto(page, url)\nclose(page)                  # …and the implicit context goes too\n\nctx = new_context(browser)   # or share one context between pages\na, b = new_page(ctx), new_page(ctx)\nclose(ctx)                   # closes both\n\nFor a page that also collects screenshots and traces when a test fails, use with_page instead of doing the bookkeeping by hand.\n\n\n\n\n\n","title":"Playwright.new_page"},{"category":"function","location":"api/#Playwright.contexts","page":"API reference","text":"contexts(browser::Browser) -> Vector{BrowserContext}\n\nThe browser's currently open BrowserContexts. Shrinks as contexts are closed, including the implicit ones new_page creates.\n\nlength(contexts(browser))   # 0 on a browser that has just launched\n\nUseful mostly as a leak check at the end of a suite: a count that only ever grows means something is not being closed.\n\n\n\n\n\n","title":"Playwright.contexts"},{"category":"function","location":"api/#Playwright.pages","page":"API reference","text":"pages(context::BrowserContext) -> Vector{Page}\n\nThe context's currently open Pages, in no guaranteed order. Shrinks as pages close.\n\npage = new_page(ctx)\nlength(pages(ctx))   # 1\n\nA popup opened by the page under test shows up here once it exists — though expect_event(ctx, :page) is the way to wait for one; see expect_event.\n\n\n\n\n\n","title":"Playwright.pages"},{"category":"function","location":"api/#Playwright.browser_name","page":"API reference","text":"browser_name(bt::BrowserType) -> String\nbrowser_name(browser::Browser) -> String\n\nEngine behind a BrowserType or a running Browser: \"chromium\" or \"firefox\".\n\nUseful for the cases where the engines genuinely differ and a test has to say so out loud rather than paper over it:\n\nif browser_name(browser) == \"firefox\"\n    # ...the one thing Firefox does differently\nend\n\nA String from both methods, not a Symbol — one name, one return type. The Browser initializer carries both name and browserName; they are identical on both engines (probed — see T8 in tasks/plan.md), and name is read because that is what the BrowserType method already used.\n\n\n\n\n\nbrowser_name(page::Page) -> String\nbrowser_name(context::BrowserContext) -> String\n\nEngine a page or context is running on, found by walking up to its Browser. This is what lets a call decide an engine-specific question client-side — pdf refuses to run off Chromium without a round trip to be told so (D7).\n\nRaises TargetClosedError when the owning browser is gone, since a page with no browser above it has been closed.\n\n\n\n\n\n","title":"Playwright.browser_name"},{"category":"function","location":"api/#Playwright.with_page","page":"API reference","text":"with_page(f, browser_or_context, url=nothing; artifacts=nothing,\n          artifacts_on=:failure, kw...)\n\nOpen a page, optionally navigate it to url, run f(page), and always close the page again. Returns whatever f returned.\n\nwith_page(browser, \"https://example.com\") do page\n    click(locator(page, \"#submit\"))\nend\n\nGiven artifacts, a directory path, report_diagnostics runs before the page closes — which is the only time it can still see anything:\n\nwith_page(browser, url; artifacts = \"artifacts/checkout\") do page\n    expect(page; to_have_title = \"Checkout\")\nend\n\nartifacts_on decides when that happens:\n\nValue Dumps\n:failure (default) only when f throws\n:always on every run\n\n:failure is the default because a screenshot per passing test is a lot of bytes for nothing on a large suite. :always exists because a passing-but-wrong run is exactly when symmetric evidence helps, and a caller who wants it should not have to reimplement the fixture. Any other value is an ArgumentError listing the two, and artifacts_on without artifacts is an ArgumentError too — it can only be a mistake about where the files were meant to go.\n\nRemaining keywords go to goto, so wait_until and timeout work as usual.\n\nnote: Your exception always wins\nIf f throws, that exception propagates unchanged — same type, same message — after the diagnostics are written. A failure in the diagnostics is a @warn, never an exception. The helper adds evidence; it never replaces the failure.\n\n\n\n\n\n","title":"Playwright.with_page"},{"category":"type","location":"api/#Playwright.Browser","page":"API reference","text":"Browser\n\nA running browser process, from launch. Holds BrowserContexts; close it to shut the process down. Ask browser_name which engine it is.\n\nplaywright() do pw\n    browser = launch(pw.chromium; headless = true)\n    try\n        page = new_page(browser)\n        goto(page, \"https://example.com\")\n    finally\n        close(browser)\n    end\nend\n\n\n\n\n\n","title":"Playwright.Browser"},{"category":"type","location":"api/#Playwright.BrowserContext","page":"API reference","text":"BrowserContext\n\nAn isolated profile inside a Browser — its own cookies, storage and permissions — from new_context. The unit of isolation between tests, and the owner of the :page, :console and :pageerror events (expect_event). Closing one closes its pages.\n\nctx = new_context(browser)\npage = new_page(ctx)          # this page's cookies are its own\nclose(ctx)                    # …and go away with the context\n\n\n\n\n\n","title":"Playwright.BrowserContext"},{"category":"type","location":"api/#Playwright.BrowserType","page":"API reference","text":"BrowserType\n\nA launchable engine: pw.chromium or pw.firefox on the handle playwright hands you. Pass it to launch.\n\nplaywright() do pw\n    for bt in (pw.chromium, pw.firefox)     # run a test on both engines\n        browser = launch(bt; headless = true)\n        # ...\n        close(browser)\n    end\nend\n\n\n\n\n\n","title":"Playwright.BrowserType"},{"category":"type","location":"api/#Playwright.Page","page":"API reference","text":"Page\n\nOne tab. The main thing you drive: goto, locator, evaluate, screenshot. Created by new_page, and also what arrives from expect_event(ctx, :page) when a popup opens.\n\nCalls on a closed page raise TargetClosedError.\n\npage = new_page(browser)\ngoto(page, \"https://example.com\")\nexpect(locator(page, \"h1\"); to_have_text = \"Example Domain\")\n\n\n\n\n\n","title":"Playwright.Page"},{"category":"type","location":"api/#Playwright.Frame","page":"API reference","text":"Frame\n\nA document within a Page — the main frame, or one per <iframe>. Get at them with frames, or scope into one with frame_locator/content_frame. Most page-level calls are frame-level calls on the main frame.\n\nfor f in frames(page)\n    @info url(f)\nend\n\n\n\n\n\n","title":"Playwright.Frame"},{"category":"type","location":"api/#Playwright.Locator","page":"API reference","text":"Locator\n\nLazy handle for selector inside a frame. Holds no element reference — every action re-resolves the selector in the browser, Playwright-style. Created with locator.\n\nstrict mirrors Playwright's strictness: when true (the default) acting on a selector that matches more than one element is an error rather than a silent pick. It is checked on action, not on construction.\n\nloc = locator(page, \"#name\")            # nothing has happened yet\nfill(loc, \"Ada\")                        # now the selector is resolved\n\nRead the three parts back with frame, selector and is_strict.\n\n\n\n\n\n","title":"Playwright.Locator"},{"category":"type","location":"api/#Playwright.ElementHandle","page":"API reference","text":"ElementHandle\n\nA reference to one specific element in the browser, from element_handle or wait_for_selector.\n\nA snapshot, unlike a Locator: it keeps pointing at that element and goes stale when the page re-renders. Prefer a locator unless you need to hold onto one element. dispose it when done.\n\nhandle = wait_for_selector(page, \"#chart\")\nevaluate(page, \"el => el.dataset.ready\", handle)\ndispose(handle)\n\n\n\n\n\n","title":"Playwright.ElementHandle"},{"category":"type","location":"api/#Playwright.JSHandle","page":"API reference","text":"JSHandle\n\nA reference to a JavaScript value kept in the browser, from evaluate_handle — for values that cannot cross the wire, like a DOM node or a closure. dispose it when done, or use the do-block form of evaluate_handle, which disposes for you.\n\nevaluate_handle(page, \"() => window.myApp\") do handle\n    evaluate(page, \"app => app.version\", handle)\nend   # disposed on the way out\n\n\n\n\n\n","title":"Playwright.JSHandle"},{"category":"function","location":"api/#Playwright.goto","page":"API reference","text":"goto(page::Page, url; timeout=nothing, wait_until=\"load\") -> Union{Response,Nothing}\n\nNavigate page to url and wait for the navigation to finish.\n\nwait_until decides what counts as finished, cheapest first:\n\nwait_until Returns once\n\"commit\" the response headers are in and navigation is committed\n\"domcontentloaded\" the DOMContentLoaded event fired\n\"load\" (default) the load event fired — subresources included\n\"networkidle\" the network has been quiet for 500ms\n\n\"networkidle\" is tempting and usually the wrong choice: on a page that polls, it never arrives. Prefer the default and then assert on what you actually need with expect, which waits for that one thing rather than for the whole page to go quiet.\n\ngoto(page, \"http://127.0.0.1:8000/\")\nexpect(page; to_have_title = \"Home\")\n\ntimeout defaults to the navigation timeout in force for page — see set_default_navigation_timeout! — and running out of it raises TimeoutError.\n\n\n\n\n\n","title":"Playwright.goto"},{"category":"function","location":"api/#Playwright.title","page":"API reference","text":"title(page::Page) -> String\n\nTitle of the document currently loaded in page.\n\nA read, taken now. The assertion form waits, and is what a test should use:\n\ntitle(page)                                   # \"Home\"\nexpect(page; to_have_title = \"Home\")          # waits for it to become \"Home\"\n\nSee expect; the URL equivalent is url.\n\n\n\n\n\n","title":"Playwright.title"},{"category":"function","location":"api/#Playwright.url","page":"API reference","text":"url(frame::Frame) -> String\n\nThe URL currently loaded in frame, kept up to date as it navigates. For a page, ask its main frame — or assert on it, which waits:\n\nurl(main_frame(page))\nexpect(page; to_have_url = r\"/checkout$\")   # the assertion form\n\nSee expect, and title for the document's title.\n\n\n\n\n\n","title":"Playwright.url"},{"category":"function","location":"api/#Playwright.screenshot","page":"API reference","text":"screenshot(page::Page; path=nothing, timeout=nothing) -> Vector{UInt8}\n\nCapture a PNG screenshot of page, returning its bytes and writing them to path when given. The bytes come back either way, so a screenshot can be attached to a report without ever touching the filesystem.\n\nscreenshot(page; path = \"artifacts/checkout.png\")\n\nbytes = screenshot(page)          # …or keep it in memory\nlength(bytes)                     # a PNG, magic bytes and all\n\nThe viewport only — this package does not expose full-page or element screenshots. timeout defaults to the action timeout in force for page — see set_default_timeout!. For a failing test, with_page captures one automatically, and pdf is the Chromium-only print equivalent.\n\n\n\n\n\n","title":"Playwright.screenshot"},{"category":"function","location":"api/#Playwright.pdf","page":"API reference","text":"pdf(page::Page; path=nothing, kwargs...) -> Vector{UInt8}\n\nRender page to PDF, returning the bytes and writing them to path when given — the same shape as screenshot.\n\nbytes = pdf(page; path = \"artifacts/page.pdf\", format = \"A4\")\n\nOption Meaning\nformat paper size, e.g. \"A4\" or \"Letter\"\nwidth, height explicit paper size, as CSS lengths; override format\nlandscape rotate the paper\nmargin a NamedTuple or Dict of top/bottom/left/right\nprint_background include background graphics, off by default upstream\nscale render scale, 0.1–2.0\npage_ranges e.g. \"1-3, 8\"\nprefer_css_page_size let the page's own @page size win\ndisplay_header_footer, header_template, footer_template running headers\noutline, tagged PDF outline and accessibility tags\n\nOptions left unset are omitted from the protocol message entirely, so the driver's own defaults apply.\n\nwarning: Chromium only\npage.pdf exists only on Chromium upstream. On any other engine this raises an ArgumentError naming the engine — decided client-side from browser_name, so it costs no round trip and the message says what is actually wrong rather than surfacing a driver error about an unknown command.\n\n\n\n\n\n","title":"Playwright.pdf"},{"category":"function","location":"api/#Playwright.locator","page":"API reference","text":"locator(page::Page, selector; strict=true) -> Locator\nlocator(frame::Frame, selector; strict=true) -> Locator\n\nLazy handle for selector (CSS, text=, xpath=, …). The selector is re-resolved in the browser on every action.\n\nWith strict=true (the default, matching Playwright) acting on a selector that matches more than one element raises a PlaywrightError rather than silently picking one. Pass strict=false when you mean to work with several matches, and reach for count, nth or iteration.\n\nsliders = locator(page, \"input[type=range]\"; strict=false)\ncount(sliders)      # 2\n\n\n\n\n\nlocator(fl::FrameLocator, selector; strict=true) -> Locator\n\nAddress an element inside the iframe fl refers to. The frame boundary is crossed by the selector itself, so the result is an ordinary Locator that resolves the whole chain on every action.\n\n\n\n\n\n","title":"Playwright.locator"},{"category":"function","location":"api/#Playwright.nth","page":"API reference","text":"nth(loc::Locator, i) -> Locator\n\nThe i-th match of loc, 1-based (converted to Playwright's 0-based nth= on the wire). The result is itself strict: it matches exactly one element by construction, whatever loc's own strictness.\n\nrows = locator(page, \"tr\"; strict = false)\ntext_content(nth(rows, 2))    # the second row; `rows[2]` is the same thing\n\nSee locator for strictness, and first / last for the two common cases.\n\nOut-of-range indices are not detected here — like any locator, nth resolves when it is acted upon, and acting on a non-existent match raises then.\n\n\n\n\n\n","title":"Playwright.nth"},{"category":"function","location":"api/#Playwright.selector","page":"API reference","text":"selector(loc::Locator) -> String\n\nThe selector string loc was built with, including any >> nth= suffix added by nth or iteration.\n\nselector(nth(locator(page, \"li\"; strict = false), 3))   # \"li >> nth=2\"\n\n\n\n\n\n","title":"Playwright.selector"},{"category":"function","location":"api/#Playwright.frame","page":"API reference","text":"frame(loc::Locator) -> Frame\n\nThe Frame loc resolves against. Every locator belongs to exactly one frame — the main frame when it was built from a Page.\n\nframe(locator(page, \"h1\")) === main_frame(page)   # true\n\n\n\n\n\n","title":"Playwright.frame"},{"category":"function","location":"api/#Playwright.is_strict","page":"API reference","text":"is_strict(loc::Locator) -> Bool\n\nWhether loc raises when its selector matches more than one element. See locator for what strictness costs and buys.\n\nis_strict(locator(page, \"li\"))                   # true, the default\nis_strict(locator(page, \"li\"; strict = false))   # false\n\n\n\n\n\n","title":"Playwright.is_strict"},{"category":"function","location":"api/#Playwright.text_content","page":"API reference","text":"text_content(loc::Locator; timeout=nothing) -> Union{String,Nothing}\n\nThe textContent of the matched element (nothing for elements without one). Includes text that is not rendered; see inner_text for what a user would actually see.\n\nThis reads once and returns. To assert on text, reach for expect instead — it retries, so it passes as soon as late-arriving text arrives rather than failing on the first look.\n\ntext_content(locator(page, \"#status\"))              # a read\nexpect(locator(page, \"#status\"); to_have_text = \"ready\")   # an assertion\n\n\n\n\n\n","title":"Playwright.text_content"},{"category":"function","location":"api/#Playwright.inner_text","page":"API reference","text":"inner_text(loc::Locator; timeout=nothing) -> String\n\nThe innerText of the matched element: the rendered text, so hidden elements and collapsed whitespace are excluded. Contrast text_content, which returns the raw text including hidden nodes.\n\n# <p>visible <span style=\"display:none\">hidden</span></p>\ninner_text(locator(page, \"p\"))     # \"visible\"\ntext_content(locator(page, \"p\"))   # \"visible hidden\"\n\n\n\n\n\n","title":"Playwright.inner_text"},{"category":"function","location":"api/#Playwright.inner_html","page":"API reference","text":"inner_html(loc::Locator; timeout=nothing) -> String\n\nThe innerHTML of the matched element — its markup, not its text. For text use inner_text or text_content.\n\ninner_html(locator(page, \"#log\"))   # \"<li class=\"greeting\">Hello</li>\"\n\n\n\n\n\n","title":"Playwright.inner_html"},{"category":"function","location":"api/#Playwright.get_attribute","page":"API reference","text":"get_attribute(loc::Locator, name; timeout=nothing) -> Union{String,Nothing}\n\nThe matched element's name attribute, or nothing when it has none. An attribute present but empty gives \"\", which is not the same answer.\n\nget_attribute(locator(page, \"#link\"), \"href\")     # \"/somewhere\"\nget_attribute(locator(page, \"#link\"), \"nope\")     # nothing\n\nTo assert on an attribute rather than read it, use expect(loc; to_have_attribute = \"href\" => \"/somewhere\") — see expect — which retries while this does not.\n\n\n\n\n\n","title":"Playwright.get_attribute"},{"category":"function","location":"api/#Playwright.input_value","page":"API reference","text":"input_value(loc::Locator; timeout=nothing) -> String\n\nCurrent value of the matched input, textarea or select element. This is the live value, which is not the same as the value attribute the HTML was served with — see get_attribute for that one.\n\nfill(locator(page, \"#name\"), \"Ada\")\ninput_value(locator(page, \"#name\"))   # \"Ada\"\n\nThe assertion form is expect(loc; to_have_value = \"Ada\") — see expect.\n\n\n\n\n\n","title":"Playwright.input_value"},{"category":"function","location":"api/#Playwright.is_visible","page":"API reference","text":"is_visible(loc::Locator) -> Bool\n\nWhether the matched element is visible. Returns false rather than raising when nothing matches, so it is safe to ask about elements that may not exist.\n\nIt answers about now, with no waiting, which makes it the wrong tool for \"has it appeared yet\" — that is expect's job.\n\nis_visible(locator(page, \"#banner\"))                   # false, maybe not yet\nexpect(locator(page, \"#banner\"); to_be_visible = true) # waits for it\n\n\n\n\n\n","title":"Playwright.is_visible"},{"category":"function","location":"api/#Playwright.is_checked","page":"API reference","text":"is_checked(loc::Locator; timeout=nothing) -> Bool\n\nWhether the matched checkbox or radio input is checked.\n\nclick(locator(page, \"#accept\"))\nis_checked(locator(page, \"#accept\"))   # true\n\nAs with is_visible, this reads rather than waits; for an assertion use expect(loc; to_be_checked = true) — see expect.\n\n\n\n\n\n","title":"Playwright.is_checked"},{"category":"function","location":"api/#Playwright.is_enabled","page":"API reference","text":"is_enabled(loc::Locator; timeout=nothing) -> Bool\n\nWhether the matched element is enabled (not disabled).\n\nis_enabled(locator(page, \"button#submit\"))   # false while the form is invalid\n\nFor an assertion that waits for a button to become enabled, use expect(loc; to_be_enabled = true) — see expect.\n\n\n\n\n\n","title":"Playwright.is_enabled"},{"category":"function","location":"api/#Playwright.click","page":"API reference","text":"click(loc::Locator; timeout=nothing)\n\nClick the matched element, waiting for it to be actionable first: attached, visible, stable, able to receive events and not disabled. That wait is why a click at the right moment needs no sleep before it.\n\nclick(locator(page, \"#greet\"))\nexpect(locator(page, \"li.greeting\"); to_have_text = \"Hello!\")\n\ntimeout covers the whole wait and defaults to the set_default_timeout! cascade; a click that never becomes actionable raises TimeoutError. For a synthetic event with none of those checks, see dispatch_event.\n\n\n\n\n\n","title":"Playwright.click"},{"category":"function","location":"api/#Playwright.dispatch_event","page":"API reference","text":"dispatch_event(loc::Locator, type, event_init=missing; timeout=nothing)\n\nDispatch a DOM event of type (\"input\", \"change\", \"click\", …) on the matched element. event_init is serialized with the same mapping as evaluate arguments and becomes the event's initializer.\n\nUnlike click this is a synthetic event, not a real user gesture: no actionability checks run and the element is not scrolled into view. That makes it the tool for driving controls that ordinary interaction cannot hit precisely — setting an input[type=range] to an exact value, for example:\n\neval_on_selector(page, \"#volume\", \"(el, v) => el.value = v\", 7)\ndispatch_event(locator(page, \"#volume\"), \"input\")   # let listeners react\n\n\n\n\n\n","title":"Playwright.dispatch_event"},{"category":"function","location":"api/#Playwright.element_handle","page":"API reference","text":"element_handle(loc::Locator) -> Union{ElementHandle,Nothing}\n\nResolve loc now and return an ElementHandle for the match, or nothing if there is none.\n\nhandle = element_handle(locator(page, \"#chart\"))\nevaluate(page, \"el => el.getBoundingClientRect().width\", handle)\ndispose(handle)\n\nThis is a snapshot, and that is the whole difference from a locator: the handle keeps pointing at that element, so it goes stale if the page re-renders, whereas a locator re-resolves on every use. Prefer the locator unless you specifically need to hold on to one element — for instance to pass it into evaluate as an argument. Handles should be disposed when you are done with them.\n\nTo wait for an element that is not there yet, use wait_for_selector instead — this does not wait.\n\n\n\n\n\n","title":"Playwright.element_handle"},{"category":"function","location":"api/#Playwright.frames","page":"API reference","text":"frames(page::Page) -> Vector{Frame}\n\nEvery Frame in page: the main frame first, then its descendants in breadth-first order. Frames detached at runtime drop out of this list.\n\nlength(frames(page))                  # 2 for a page with one iframe\n[url(f) for f in frames(page)]\n\nThis is for inspecting the frame tree. To act on elements inside an iframe, scope into it with frame_locator instead — that does not care where the frame sits in this list.\n\n\n\n\n\n","title":"Playwright.frames"},{"category":"function","location":"api/#Playwright.parent_frame","page":"API reference","text":"parent_frame(frame::Frame) -> Union{Frame,Nothing}\n\nThe Frame containing frame, or nothing for a page's main frame — which is how you tell the main frame apart from the rest.\n\nmain = only(filter(f -> parent_frame(f) === nothing, frames(page)))\n\nThe downward direction is frames; from an element, it is owner_frame.\n\n\n\n\n\n","title":"Playwright.parent_frame"},{"category":"function","location":"api/#Playwright.owner_frame","page":"API reference","text":"owner_frame(loc::Locator) -> Union{Frame,Nothing}\n\nThe frame that contains the element loc matches — the inverse of content_frame, which returns the frame an element contains. nothing when the locator matches no element.\n\ninner = locator(frame_locator(page, \"#embed\"), \"h1\")\nowner_frame(inner) === main_frame(page)    # false: it lives in the iframe\n\nBoth of these resolve the locator immediately, so neither waits; see wait_for_selector for an element that is not there yet.\n\n\n\n\n\n","title":"Playwright.owner_frame"},{"category":"function","location":"api/#Playwright.frame_locator","page":"API reference","text":"frame_locator(page::Page, selector) -> FrameLocator\nframe_locator(frame::Frame, selector) -> FrameLocator\nframe_locator(fl::FrameLocator, selector) -> FrameLocator\n\nScope into the iframe matched by selector. Elements inside it are addressed by chaining a locator:\n\ninner = frame_locator(page, \"iframe#child\")\nclick(locator(inner, \"button\"))\nevaluate(content_frame(inner), \"document.title\")\n\nNothing is resolved until the resulting locator is used, so this can be built before the iframe has loaded. The FrameLocator form scopes into an iframe nested inside another iframe.\n\n\n\n\n\n","title":"Playwright.frame_locator"},{"category":"function","location":"api/#Playwright.content_frame","page":"API reference","text":"content_frame(fl::FrameLocator) -> Frame\ncontent_frame(loc::Locator) -> Frame\n\nThe frame an iframe element contains, so it can be evaluated in or enumerated directly:\n\nevaluate(content_frame(frame_locator(page, \"iframe\")), \"document.title\")\n\nRaises a PlaywrightError when the selector matches nothing, or matches an element that is not a frame.\n\n\n\n\n\n","title":"Playwright.content_frame"},{"category":"function","location":"api/#Playwright.name","page":"API reference","text":"name(frame::Frame) -> String\n\nThe frame's name attribute, or \"\" when it has none — an unnamed frame and a frame named \"\" are indistinguishable here.\n\nfindfirst(f -> name(f) == \"checkout\", frames(page))\n\nNames are a convenience for finding a known frame; frame_locator is the way to actually work inside one. Note this is unrelated to browser_name.\n\n\n\n\n\n","title":"Playwright.name"},{"category":"function","location":"api/#Playwright.evaluate","page":"API reference","text":"evaluate(loc::Locator, expression, arg=missing; is_function=nothing) -> Any\n\nEvaluate expression with the element loc matches as its first argument, and return the result converted to Julia.\n\nThe locator's own selector and strict flag are used, so a strict locator matching several elements raises here just as it would for click. This is the precise tool for controls that ordinary interaction cannot drive exactly — pair it with dispatch_event so the page's listeners still run:\n\nslider = locator(page, \"#volume\")\nevaluate(slider, \"(el, v) => el.value = v\", 7)\ndispatch_event(slider, \"input\")\n\nRaises a PlaywrightError when nothing matches. See evaluate_all for the every-match form.\n\n\n\n\n\nevaluate(target, expression, arg=missing; is_function=nothing) -> Any\n\nEvaluate expression in target (a Page, Frame or JSHandle) and return the result converted to Julia. arg is passed to the expression when it is a function; it is serialized with the same mapping as the return value.\n\nWhether expression is a function is detected by the driver, exactly as it is for Playwright's other string-based clients, so JavaScript pasted from the Playwright docs behaves the way it does there. Pass is_function=true/false to decide it yourself.\n\nNumbers arrive as Float64 — JavaScript has a single number type — so evaluate(page, \"1 + 1\") is 2.0, which still == 2.\n\nevaluate(page, \"1 + 1\")                      # 2.0\nevaluate(page, \"x => x.a * 2\", (a = 21,))    # 42.0\nevaluate(page, \"document.title\")             # \"Example Domain\"\n\n\n\n\n\n","title":"Playwright.evaluate"},{"category":"function","location":"api/#Playwright.evaluate_all","page":"API reference","text":"evaluate_all(loc::Locator, expression, arg=missing; is_function=nothing) -> Any\n\nEvaluate expression with an array of all the elements loc matches as its first argument.\n\nUnlike evaluate this never raises for a locator that matches nothing — the array is simply empty — and it ignores strict, because \"all of them\" is not an ambiguity that strictness has anything to decide.\n\nevaluate_all(locator(page, \"input\"; strict=false), \"els => els.length\")\n\n\n\n\n\n","title":"Playwright.evaluate_all"},{"category":"function","location":"api/#Playwright.evaluate_handle","page":"API reference","text":"evaluate_handle(target, expression, arg=missing; is_function=nothing) -> JSHandle\nevaluate_handle(f, target, expression, arg=missing; is_function=nothing)\n\nEvaluate expression and keep the result in the browser, returning a JSHandle that refers to it. Use this for values that cannot cross the wire (DOM nodes, functions, objects with cycles you do not want to copy).\n\nHandles hold a browser-side reference and must be released with dispose. The block form does it for you, on the way out of the block whether or not the body threw, and is the recommended way to use handles:\n\nevaluate_handle(page, \"() => window.app\") do app\n    evaluate(app, \"a => a.ready\")\nend   # disposed here, even if the body throws\n\nA handle can be passed straight back in as arg to a later evaluate.\n\n\n\n\n\n","title":"Playwright.evaluate_handle"},{"category":"function","location":"api/#Playwright.eval_on_selector","page":"API reference","text":"eval_on_selector(target, selector, expression, arg=missing; is_function=nothing, strict=true) -> Any\n\nEvaluate expression with the element matching selector as its first argument, and return the result converted to Julia. Raises a PlaywrightError when nothing matches.\n\neval_on_selector(page, \"#name\", \"el => el.value\")\neval_on_selector(page, \"#volume\", \"(el, v) => el.value = v\", 7)\n\narg is serialized with the same mapping as evaluate arguments, and strict behaves as it does for a locator. The locator-first form is usually what you want — evaluate(loc, …), see evaluate — since a locator already carries both the selector and the strictness; this one exists for the times you have a selector and nothing else. The all-matches form is eval_on_selector_all.\n\n\n\n\n\n","title":"Playwright.eval_on_selector"},{"category":"function","location":"api/#Playwright.eval_on_selector_all","page":"API reference","text":"eval_on_selector_all(target, selector, expression, arg=missing; is_function=nothing) -> Any\n\nEvaluate expression with an array of all elements matching selector as its first argument. Unlike eval_on_selector this never raises for a selector that matches nothing — the array is simply empty.\n\neval_on_selector_all(page, \"input\", \"els => els.length\")\n\n\n\n\n\n","title":"Playwright.eval_on_selector_all"},{"category":"function","location":"api/#Playwright.dispose","page":"API reference","text":"dispose(handle::JSHandle)\n\nRelease a handle's browser-side reference. Handles left undisposed are reclaimed when their page or context closes, so leaking one costs memory rather than correctness — but a long-lived page can accumulate them.\n\nhandle = evaluate_handle(page, \"() => document.body\")\ntry\n    evaluate(page, \"el => el.childElementCount\", handle)\nfinally\n    dispose(handle)\nend\n\nThe block form of evaluate_handle does this for you and is the recommended way to use handles. Applies to ElementHandle too, which element_handle and wait_for_selector return.\n\nPrefer the block form of evaluate_handle, which disposes for you.\n\n\n\n\n\n","title":"Playwright.dispose"},{"category":"function","location":"api/#Playwright.expect","page":"API reference","text":"expect(loc::Locator; timeout=nothing, matchers...) -> loc\n\nAssert something about loc, retrying in the browser until it holds or timeout runs out. Returns loc on success, so assertions chain; raises AssertionFailure on failure, with a message naming both what was expected and what was actually there.\n\nThis is what replaces sleep-then-assert. The condition is re-checked driver-side, so an element that arrives late passes as soon as it arrives rather than after a fixed wait:\n\nexpect(locator(page, \"#late\"); to_have_text = \"late arrival\")\nexpect(locator(page, \"li\"; strict=false); to_have_count = 3)\nexpect(locator(page, \"#box\"); to_have_value = r\"^se\")\nexpect(locator(page, \"#link\"); to_have_attribute = \"href\" => \"/somewhere\")\n\nMatcher Expects\nto_have_text exact text — a String or a Regex\nto_contain_text text containing the given substring or match\nto_have_value an input's value\nto_have_count how many elements the locator matches\nto_have_attribute name => value\nto_be_visible, to_be_hidden true/false\nto_be_enabled, to_be_disabled true/false\nto_be_checked true/false\n\nWrap any expectation in Not to negate it; for the boolean matchers, = false does the same thing more readably.\n\nSeveral matchers in one call are checked one after another, and the first failure raises. timeout is in milliseconds and defaults to the set_default_timeout! cascade — and applies to each matcher, since each is its own retrying check.\n\nFor a condition this cannot express, retry_until is the escape hatch.\n\n\n\n\n\nexpect(page::Page; timeout=nothing, matchers...) -> page\nexpect(frame::Frame; timeout=nothing, matchers...) -> frame\n\nAssert something about the document rather than about an element, retrying in the browser until it holds or timeout runs out. Returns its target, so assertions chain; raises AssertionFailure on failure, carrying the value that was actually there.\n\nexpect(page; to_have_title = \"M4\")\nexpect(page; to_have_url = r\"m4\\.html$\")\n\nMatcher Expects\nto_have_title the document title — a String or a Regex\nto_have_url the current URL — a String or a Regex\n\nexpect(page; …) delegates to the page's main frame; pass a Frame directly to assert about an iframe's document instead.\n\nMatchers are partitioned by target: the element matchers (to_have_text, to_be_visible, …) belong to expect(::Locator) and passing one here is an ArgumentError naming the target that would work. That check is local on purpose — sent to the driver, a matcher against the wrong target fails with the same generic message a real mismatch produces.\n\n\n\n\n\n","title":"Playwright.expect"},{"category":"type","location":"api/#Playwright.Not","page":"API reference","text":"Not(expected)\n\nNegate a single expect matcher:\n\nexpect(loc; to_have_text = Not(\"Goodbye\"))     # any text but that\n\nA wrapper rather than a call-wide negate keyword, because one expect call can carry several matchers and each is a separate check — with a single flag there would be no way to say \"has count 3, but not this text\".\n\nIt is an ordinary value, and holds the expectation it negates:\n\njulia> n = Not(\"Goodbye\")\nNot(\"Goodbye\")\n\njulia> n.expected\n\"Goodbye\"\n\njulia> Not(r\"^se\")\nNot(r\"^se\")\n\nFor the boolean matchers, = false says the same thing more readably: to_be_visible = false rather than to_be_visible = Not(true).\n\n\n\n\n\n","title":"Playwright.Not"},{"category":"function","location":"api/#Playwright.retry_until","page":"API reference","text":"retry_until(f; timeout=nothing, interval=100, on_timeout=:throw, on_error=:throw)\nretry_until(f, target; …)\n\nCall f() every interval ms until it returns true, or give up after timeout ms.\n\nThe escape hatch for conditions expect cannot express. Prefer expect where it fits — it retries inside the browser, so it neither round-trips per attempt nor misses a state that flickers between polls:\n\nretry_until(; timeout = 5_000) do\n    length(console_messages(page)) >= 3\nend\n\nWhat happens when it does not hold\n\non_timeout Giving up means\n:throw (default) raise AssertionFailure\n:false return false\n\n:false is what makes retry_until usable inside @test. stdlib Test records a throwing assertion as an Error — \"this test is broken\" — and a false one as a Fail — \"this assertion did not hold\", which is the truth about a condition that never arrived, and which renders the expression and its value instead of a stacktrace through package internals:\n\n@test retry_until(page; on_timeout = :false) do\n    HTTP.get(probe_url).status == 200\nend\n\nnote: `:false` is the boolean `false`\nUnlike :throw and :retry, :false is not a Symbol — false is a boolean literal, so Julia parses :false as false. Both spellings are accepted and mean the same thing; :false is written here only because it lines up with the other values at the call site.\n\nWhat happens when the predicate throws\n\non_error An exception from f means\n:throw (default) propagate it immediately\n:retry treat it as \"not yet\" and keep going, under the same deadline\n\n:throw is right for a DOM predicate, where a throw is a broken test rather than a condition that has not happened yet, and swallowing it would hide the bug until the timeout. :retry is right for the common end-to-end shape where the predicate is an HTTP call against a server that is still warming up.\n\nThe silence stays bounded: under :retry the last exception is attached to the eventual timeout failure, so a predicate that never stops throwing still reports why it never stopped.\n\nWhich timeout\n\nGiven a target — a Page, BrowserContext, Frame or Locator, positionally, as expect_event takes one — the timeout comes from the set_default_timeout! cascade, so retry_until follows the same rules as everything else:\n\nset_default_timeout!(page, 2_000)\nretry_until(() -> ready(), page)      # gives up after 2 s\n\nWithout a target there is nothing to inherit from and timeout falls back to DEFAULT_TIMEOUT. An explicit timeout keyword always wins.\n\n\n\n\n\n","title":"Playwright.retry_until"},{"category":"function","location":"api/#Playwright.wait_for_selector","page":"API reference","text":"wait_for_selector(target, selector; state=nothing, strict=true, timeout=nothing)\nwait_for_selector(loc::Locator; state=nothing, timeout=nothing)\n\nWait until selector reaches state in target (a Page or Frame), and return the matching ElementHandle.\n\nstate is one of:\n\nState Waits for the element to be\n:attached present in the DOM (the driver's default)\n:detached absent from the DOM\n:visible present and non-empty with no visibility: hidden\n:hidden absent, or present but not visible\n\n:detached and :hidden return nothing — there is no element to hand back.\n\ntimeout is in milliseconds and defaults to the set_default_timeout! cascade. A selector that never arrives raises TimeoutError, which is what makes \"the element is late\" distinguishable from \"the page threw\".\n\nel = wait_for_selector(page, \"#late\"; state=:visible)\ntext_content(locator(page, \"#late\"))\n\nPassing a Locator instead uses its own selector and strictness:\n\nwait_for_selector(locator(page, \"#late\"); state=:visible)\n\n\n\n\n\n","title":"Playwright.wait_for_selector"},{"category":"function","location":"api/#Playwright.wait_for_function","page":"API reference","text":"wait_for_function(target, expression, arg=missing; polling=:raf, timeout=nothing, is_function=nothing)\n\nWait until expression returns a truthy value in target (a Page, Frame or Locator), and return a JSHandle for that value.\n\narg is passed to the expression when it is a function, serialized with the same mapping as evaluate.\n\npolling is :raf (the default — re-check on every animation frame, which is right for anything the browser paints) or a number of milliseconds for a fixed interval, which is better for a condition that is not tied to rendering.\n\ntimeout defaults to the set_default_timeout! cascade. A predicate that never becomes true raises TimeoutError; a predicate that throws raises DriverError — the distinction between a condition that has not happened yet and one that can never happen.\n\nwait_for_function(page, \"() => window.ready\")\nwait_for_function(page, \"n => document.querySelectorAll('li').length > n\", 3)\n\n\n\n\n\n","title":"Playwright.wait_for_function"},{"category":"function","location":"api/#Playwright.expect_event","page":"API reference","text":"expect_event(f, target, event; timeout=nothing, predicate=nothing) -> payload\n\nRun f() and return the first event on target that it produces.\n\nThe subscription is attached before f runs, so an event the body fires synchronously is still caught — which is why this takes a block rather than letting you subscribe and act in two statements:\n\npopup = expect_event(ctx, :page) do\n    click(locator(page, \"#open-popup\"))\nend\ntitle(popup)\n\nf's own return value is discarded; the event payload is what comes back. An exception from f propagates unchanged, and the subscription is released either way.\n\npredicate filters payloads — the first one it accepts is returned. timeout is in milliseconds and defaults to the set_default_timeout! cascade; TimeoutError is raised if no matching event arrives.\n\nSupported events, by owner:\n\nOwner Event Payload\nPage :close, :crash the Page\nPage :frameattached, :framedetached Frame\nBrowserContext :page Page — this is how you catch a popup\nBrowserContext :close the BrowserContext\nBrowserContext :console ConsoleMessage\nBrowserContext :pageerror PageError\n\nAnything else raises ArgumentError. Network events (:request, :response, …) and :dialog, :download and friends are deferred rather than designed away — their payload types have no accessors yet.\n\nSee also wait_for_event and with_events.\n\n\n\n\n\n","title":"Playwright.expect_event"},{"category":"function","location":"api/#Playwright.wait_for_event","page":"API reference","text":"wait_for_event(target, event; timeout=nothing, predicate=nothing) -> payload\n\nWait for the next event on target without running anything first.\n\nUse this only when whatever triggers the event is already in flight — a page closing on its own, say. When you trigger it, use expect_event: subscribing after the trigger is a race this cannot protect you from.\n\nSame events, payloads, predicate and timeout as expect_event.\n\n# The page closes itself after a countdown that is already running.\nwait_for_event(page, :close; timeout = 10_000)\n\nRaises TimeoutError if nothing arrives in time.\n\n\n\n\n\n","title":"Playwright.wait_for_event"},{"category":"function","location":"api/#Playwright.with_events","page":"API reference","text":"with_events(f, target, event) -> f's return value\n\nRun f(stream) with a live EventStream of every event on target, for collecting several events rather than waiting for one:\n\nerrors = with_events(ctx, :pageerror) do stream\n    click(locator(page, \"#break-everything\"))\n    sleep(0.5)\n    pending_events(stream)\nend\n\nThe buffer is unbounded and attached before f runs, so nothing that happens inside the block is missed. It is released when the block ends, however it ends. Read the stream with next_event or pending_events.\n\n\n\n\n\n","title":"Playwright.with_events"},{"category":"type","location":"api/#Playwright.EventStream","page":"API reference","text":"EventStream\n\nLive handle to the events of one kind on one owner, handed to a with_events block. Read from it with next_event (blocking, one at a time) or pending_events (everything buffered right now).\n\nThe buffer is unbounded, so nothing that happens inside the block is dropped while you are not looking. It is closed when the block ends.\n\nwith_events(ctx, :pageerror) do stream\n    click(locator(page, \"#break-everything\"))\n    next_event(stream; timeout = 5_000).message\nend\n\nlength(stream) is the non-consuming count, which is the thing to poll on — see length.\n\n\n\n\n\n","title":"Playwright.EventStream"},{"category":"function","location":"api/#Playwright.next_event","page":"API reference","text":"next_event(stream::EventStream; timeout=nothing, predicate=nothing) -> payload\n\nTake the next event off stream, waiting up to timeout ms for one to arrive. Raises TimeoutError if none does. timeout defaults to the set_default_timeout! cascade.\n\npredicate skips payloads it returns false for; they are consumed, not requeued — so a rejected payload is gone, not left for the next call.\n\nwith_events(page, :console) do stream\n    click(locator(page, \"#log\"))\n    msg = next_event(stream; predicate = m -> m.type == \"error\")\n    @info \"first console error\" msg.text\nend\n\nBlocking and one at a time; pending_events is the drain-everything form, and EventStream has the pair side by side.\n\n\n\n\n\n","title":"Playwright.next_event"},{"category":"function","location":"api/#Playwright.pending_events","page":"API reference","text":"pending_events(stream::EventStream) -> Vector\n\nEverything buffered right now, in arrival order. Drains the stream: a second call returns only what arrived since the first, which is what makes it usable in a loop. Does not block and does not wait for more — to wait for a particular count, poll length first.\n\nwith_events(ctx, :pageerror) do stream\n    click(locator(page, \"#break-everything\"))\n    retry_until(() -> length(stream) >= 2; timeout = 5_000)\n    pending_events(stream)         # both of them, and the buffer is now empty\nend\n\nFor one event at a time, blocking, use next_event; see EventStream.\n\n\n\n\n\n","title":"Playwright.pending_events"},{"category":"type","location":"api/#Playwright.Artifact","page":"API reference","text":"Artifact\n\nA file the driver is producing — a trace zip from stop_tracing or a video recording. Three verbs: path blocks until it is completely written and says where it is, save_as copies it somewhere of your choosing, and delete removes it.\n\nThe distinction worth knowing: the driver knows the eventual path immediately, but the file is only complete later — a video not until its page or context closes. path is what waits.\n\nartifact = stop_tracing(ctx)\nsave_as(artifact, \"artifacts/trace.zip\")   # blocks until fully written\n\n\n\n\n\n","title":"Playwright.Artifact"},{"category":"function","location":"api/#Playwright.start_tracing","page":"API reference","text":"start_tracing(ctx::BrowserContext; screenshots=true, snapshots=true,\n              sources=false, name=nothing, title=nothing)\n\nBegin recording a Playwright trace on ctx — the full record of what the browser did, viewable afterwards in Playwright's own trace viewer. Pair with stop_tracing, or use with_tracing to guarantee the pairing even when the block throws, which is the run worth tracing.\n\nstart_tracing(ctx; title = \"checkout\")\ntry\n    goto(page, url)\n    click(locator(page, \"#submit\"))\nfinally\n    stop_tracing(ctx; path = \"artifacts/trace.zip\")\nend\n\nOption Records\nscreenshots a screencast, so the trace viewer has a filmstrip\nsnapshots DOM snapshots, so actions can be inspected before/after\nname, title naming for the trace and the chunk\n\nTwo protocol calls, not one: the recording is configured, then a chunk is opened. stop_tracing closes the chunk, and a stop with no chunk open has nothing to archive.\n\nnote: `sources` is not available on this path\nUpstream clients embed calling source files by passing includeSources to localUtils.zip, which they use because they assemble the zip themselves. This package lets the driver assemble it (tracingStopChunk(mode=\"archive\") — see D1), and the 1.61.1 tracingStart protocol carries no sources flag. Passing sources = true therefore raises an ArgumentError rather than being silently dropped: a flag that quietly does nothing is worse than one that is not offered.\n\n\n\n\n\n","title":"Playwright.start_tracing"},{"category":"function","location":"api/#Playwright.stop_tracing","page":"API reference","text":"stop_tracing(ctx::BrowserContext; path) -> path\n\nStop the recording started by start_tracing and write the trace zip to path, returning path. Parent directories are created as needed.\n\nstart_tracing(ctx)\ngoto(page, url)\nstop_tracing(ctx; path = \"artifacts/trace.zip\")\n\nEach stop consumes the chunk start_tracing opened, so tracing a second run means calling start_tracing again. with_tracing does both halves for you. Open the result with:\n\nnpx playwright@1.61.1 show-trace artifacts/trace.zip\n\nThe zip is assembled by the driver, not by Julia — this package has no zip dependency and does not parse the trace. It is an opaque artifact for the upstream viewer.\n\n\n\n\n\n","title":"Playwright.stop_tracing"},{"category":"function","location":"api/#Playwright.with_tracing","page":"API reference","text":"with_tracing(f, ctx::BrowserContext; path, screenshots=true, snapshots=true,\n             sources=false, name=nothing, title=nothing)\n\nRecord a Playwright trace around f() and write it to path.\n\nThe zip is written however the block exits — that is the point, since the run worth tracing is the one that threw:\n\nwith_tracing(ctx; path = \"artifacts/trace.zip\") do\n    goto(page, url)\n    click(locator(page, \"#submit\"))\nend\n\nReturns whatever f() returned. Open the trace with npx playwright show-trace artifacts/trace.zip.\n\nOptions other than path are passed straight to start_tracing; the zip is written by stop_tracing. For a whole test wrapped in a trace and a screenshot on failure, see with_page.\n\nnote: A failed save never replaces your exception\nSaving the trace happens on the teardown path, while a more important error may already be in flight. If it fails it is reported with @warn and the block's own exception — or its return value — is what reaches the caller. A helper that adds evidence must never destroy the thing it was called to explain.\n\n\n\n\n\n","title":"Playwright.with_tracing"},{"category":"function","location":"api/#Playwright.video","page":"API reference","text":"video(page::Page) -> Union{Artifact,Nothing}\n\nThe video being recorded for page, or nothing if its context was not created with record_video.\n\nRecording is switched on per context, not per page, because that is what the protocol offers:\n\nctx = new_context(browser; record_video = (dir = \"artifacts/video\",))\npage = new_page(ctx)\ngoto(page, url)\n\nclose(page)                          # ...the file is finished by this\n@test isfile(path(video(page)))\n\nwarning: The file does not exist until the page or context closes\nThis is an upstream sharp edge, not a Playwright.jl one. The video is still being written while the page is open, so path(video(page)) called too early blocks until the recording is finalized — and a naive test that asserts isfile before closing sees nothing and looks like a bug in this package. Close the page first, as above.\n\nThe result is an ordinary Artifact, so path, save_as and delete all work on it — save_as being the way to move it somewhere of your choosing rather than the driver's temporary directory.\n\n\n\n\n\n","title":"Playwright.video"},{"category":"function","location":"api/#Playwright.save_as","page":"API reference","text":"save_as(a::Artifact, path::AbstractString) -> path\n\nCopy a driver-side artifact — a trace zip, a video — to path, blocking until it has finished being written. Returns path, so it composes.\n\nsave_as(video(page), \"artifacts/run.webm\")\n\nParent directories are created if they do not exist, because the caller supplying \"artifacts/run.webm\" on a fresh checkout means it.\n\nThe blocking is the useful part: path tells you where the driver will put the file, but a video is not finished until its page closes. Use delete for an artifact a passing test does not need to keep, and see Artifact for the three verbs together.\n\n\n\n\n\n","title":"Playwright.save_as"},{"category":"function","location":"api/#Playwright.path","page":"API reference","text":"path(a::Artifact) -> String\n\nWhere the artifact lives on disk, blocking until it is completely written.\n\nThis is not the same as the artifact's declared destination: the driver knows the eventual path immediately, but the file only becomes complete later — for video, not until the page or context closes (see video). Blocking here is the point, and is what makes\n\nclose(page)\n@test isfile(path(video(page)))\n\na test rather than a race.\n\nThe path is on the machine running the driver. That is this machine — the driver is a child process (SPEC-M4.md assumption 7) — so the file is readable from Julia directly. Use save_as to put a copy somewhere of your own choosing instead.\n\n\n\n\n\n","title":"Playwright.path"},{"category":"function","location":"api/#Playwright.delete","page":"API reference","text":"delete(a::Artifact)\n\nDelete the artifact's driver-side file. Worth calling for artifacts a passing test does not need to keep — the driver writes them into a temporary directory that lives as long as the browser does.\n\nrecording = video(page)\nclose(page)\nif test_passed\n    delete(recording)                       # nothing to look at\nelse\n    save_as(recording, \"artifacts/run.webm\")\nend\n\nThis deletes the driver's copy; a file already copied out with save_as is yours and is untouched. See Artifact.\n\n\n\n\n\n","title":"Playwright.delete"},{"category":"function","location":"api/#Playwright.console_messages","page":"API reference","text":"console_messages(page::Page) -> Vector{ConsoleMessage}\n\nEvery console message the page has produced since it opened, or since clear_console_messages was last called.\n\nfor msg in console_messages(page)\n    msg.type == \"error\" && @warn \"console error\" msg.text\nend\n\nReturns an empty vector — rather than raising TargetClosedError — if the page or its context has already closed. This is a postmortem reader, typically called from a finally block while a more important error is in flight; throwing there would mask that error, and \"the page is gone\" tells a caller who is already handling a failure nothing it can use. Any other error still propagates.\n\n\n\n\n\n","title":"Playwright.console_messages"},{"category":"function","location":"api/#Playwright.page_errors","page":"API reference","text":"page_errors(page::Page) -> Vector{PageError}\n\nEvery uncaught JavaScript error the page has raised since it opened, or since clear_page_errors was last called. Worth dumping when a test fails for a reason the assertion alone does not explain:\n\nfor err in page_errors(page)\n    @warn \"page error\" err.message err.stack\nend\n\nLike console_messages, returns an empty vector rather than raising when the page or its context has already closed — see that docstring for why.\n\n\n\n\n\n","title":"Playwright.page_errors"},{"category":"function","location":"api/#Playwright.clear_console_messages","page":"API reference","text":"clear_console_messages(page::Page)\n\nDrop the page's buffered console messages, so a later console_messages only reports what happened next. Useful for per-test isolation on a shared page, where the previous test's noise would otherwise be reported as this one's.\n\nclear_console_messages(page)\nclick(locator(page, \"#go\"))\n@test isempty(filter(m -> m.type == \"error\", console_messages(page)))\n\n\n\n\n\n","title":"Playwright.clear_console_messages"},{"category":"function","location":"api/#Playwright.clear_page_errors","page":"API reference","text":"clear_page_errors(page::Page)\n\nDrop the page's buffered uncaught errors, so a later page_errors only reports what happened next; the console equivalent is clear_console_messages.\n\nclear_page_errors(page)\nclick(locator(page, \"#go\"))\n@test isempty(page_errors(page))\n\n\n\n\n\n","title":"Playwright.clear_page_errors"},{"category":"function","location":"api/#Playwright.report_diagnostics","page":"API reference","text":"report_diagnostics(page::Page, dir::AbstractString) -> Vector{String}\n\nDump everything known about page into dir, returning the paths actually written.\n\nThree files, when they can be produced:\n\nFile From\nscreenshot.png screenshot\nconsole.log console_messages\nerrors.log page_errors\n\nfiles = report_diagnostics(page, \"artifacts/failure-42\")\n@info \"diagnostics\" files\n\nnote: It never throws\nThis is called from finally blocks, on pages that may already be closed — the exact situation where a second exception would bury the first. Anything that fails is reported with @warn and left out of the returned list, so a partial dump is still a dump. The return value is what to check if you need to know what was captured.\n\nPublic in its own right, not only as with_page's helper: a suite with its own fixture shape should not have to adopt one to get the dump.\n\n\n\n\n\n","title":"Playwright.report_diagnostics"},{"category":"type","location":"api/#Playwright.ConsoleMessage","page":"API reference","text":"ConsoleMessage\n\nOne buffered console message. type is the console method that produced it (\"log\", \"info\", \"warning\", \"error\", \"debug\", …), text is the rendered message, location is where it came from and timestamp is milliseconds since the epoch.\n\nRead them off a page with console_messages:\n\nfor msg in console_messages(page)\n    println(msg.type, \" @ \", msg.location.url, \":\", msg.location.line, \" — \", msg.text)\nend\n\n\n\n\n\n","title":"Playwright.ConsoleMessage"},{"category":"type","location":"api/#Playwright.PageError","page":"API reference","text":"PageError\n\nOne uncaught JavaScript error, with the error's message, its name (usually \"Error\") and its stack.\n\nRead them off a page with page_errors. This is a page's error — something the JavaScript threw — and so is unrelated to PlaywrightError, which is what this package's own calls raise.\n\nisempty(page_errors(page)) || @warn \"the page threw\" page_errors(page)\n\n\n\n\n\n","title":"Playwright.PageError"},{"category":"function","location":"api/#Playwright.set_default_timeout!","page":"API reference","text":"set_default_timeout!(target, milliseconds)\n\nSet the default timeout for actions on a Page or BrowserContext — clicks, locator queries, waits and assertions.\n\nThe setting is inherited: a page with no setting of its own uses its context's. An explicit timeout keyword on a call still wins over both.\n\nctx = new_context(browser)\nset_default_timeout!(ctx, 2_000)   # a missing element fails in 2 s, not 30\npage = new_page(ctx)\nset_default_timeout!(page, 5_000)  # ...but this page gets 5 s\n\nMilliseconds; 0 means no timeout. See also set_default_navigation_timeout!.\n\n\n\n\n\n","title":"Playwright.set_default_timeout!"},{"category":"function","location":"api/#Playwright.set_default_navigation_timeout!","page":"API reference","text":"set_default_navigation_timeout!(target, milliseconds)\n\nSet the default timeout for navigations (goto and friends) on a Page or BrowserContext.\n\nNavigations fall back to the set_default_timeout! setting when no navigation-specific one is in force, so setting only the action default also shortens navigations rather than leaving them at 30 s.\n\nMilliseconds; 0 means no timeout.\n\nset_default_navigation_timeout!(ctx, 60_000)   # a slow app to first paint\nset_default_timeout!(ctx, 5_000)               # but assertions stay snappy\ngoto(page, url)                                # gets the 60s budget\n\nSet on a context it applies to every page in it; set on a page it applies to that page only. A timeout keyword on goto itself beats both.\n\n\n\n\n\n","title":"Playwright.set_default_navigation_timeout!"},{"category":"type","location":"api/#Playwright.PlaywrightError","page":"API reference","text":"PlaywrightError\n\nAbstract supertype of every error surfaced from the Playwright driver.\n\nCatch this to handle any driver failure:\n\ntry\n    click(locator(page, \"#go\"))\ncatch e\n    e isa PlaywrightError || rethrow()\n    @warn \"click failed\" e.message\nend\n\nEvery concrete subtype carries the driver's message (with the call log appended, as upstream clients do), name and stack. Branch on the subtype to tell failure kinds apart:\n\nSubtype Raised when\nTimeoutError an operation exceeded its timeout\nTargetClosedError the page, context or browser closed under the call\nAssertionFailure a retrying assertion never matched\nDriverError anything else, including JS exceptions\n\nConstructing one directly is rarely useful, but it shows the shape:\n\njulia> e = DriverError(\"boom\"; name = \"Error\")\nDriverError(\"boom\", \"Error\", \"\")\n\njulia> e isa PlaywrightError\ntrue\n\njulia> e.message\n\"boom\"\n\njulia> sprint(showerror, e)\n\"DriverError: boom\"\n\njulia> TimeoutError(\"too slow\") isa PlaywrightError\ntrue\n\nnote: Changed in milestone 3\nPlaywrightError used to be a concrete struct. It is now abstract, so PlaywrightError(msg) no longer constructs — use DriverError. catch e isa PlaywrightError and e.message are unaffected.\n\n\n\n\n\n","title":"Playwright.PlaywrightError"},{"category":"type","location":"api/#Playwright.DriverError","page":"API reference","text":"DriverError(message; name=\"Error\", stack=\"\")\n\nAn unclassified driver failure — the catch-all of the taxonomy, and what a JS exception raised inside evaluate becomes. Any driver error name this package does not recognise arrives here too, so a future Playwright release cannot produce an error that escapes PlaywrightError.\n\ntry\n    evaluate(page, \"() => { throw new Error('boom') }\")\ncatch e\n    e isa DriverError    # true\n    occursin(\"boom\", e.message)\nend\n\n\n\n\n\n","title":"Playwright.DriverError"},{"category":"type","location":"api/#Playwright.TimeoutError","page":"API reference","text":"TimeoutError(message; name=\"Error\", stack=\"\")\n\nAn operation exceeded its timeout: a selector that never appeared, a navigation that never settled, a retrying assertion's underlying wait.\n\nDistinguishing this from DriverError is the point of the taxonomy — a selector that is merely late is a different problem from a page that threw.\n\ntry\n    click(locator(page, \"#never\"); timeout = 1_000)\ncatch e\n    e isa TimeoutError && @info \"still not there after a second\"\nend\n\nThe timeout that ran out is the one resolved by the set_default_timeout! cascade. To retry on a timeout rather than raise, see retry_until's on_timeout option.\n\n\n\n\n\n","title":"Playwright.TimeoutError"},{"category":"type","location":"api/#Playwright.TargetClosedError","page":"API reference","text":"TargetClosedError(message; name=\"Error\", stack=\"\")\n\nThe page, context or browser the call targeted was closed underneath it.\n\nThis is a lifecycle error rather than a failure of the call: the usual cause is a close in a finally racing work still in flight, or acting on a page whose context has already gone.\n\nclose(page)\ntry\n    title(page)\ncatch e\n    e isa TargetClosedError    # true\nend\n\nSee PlaywrightError for the rest of the taxonomy.\n\n\n\n\n\n","title":"Playwright.TargetClosedError"},{"category":"type","location":"api/#Playwright.AssertionFailure","page":"API reference","text":"AssertionFailure(message; name=\"Error\", stack=\"\")\n\nA retrying assertion never matched within its timeout. The message carries both the expected and the received value, which is the difference between this and a bare TimeoutError — an assertion that fails should say what it saw.\n\nRaised by expect:\n\ntry\n    expect(locator(page, \"h1\"); to_have_text = \"Wrong\", timeout = 1_000)\ncatch e\n    e isa AssertionFailure\n    @info e.message    # names both the expected and the received text\nend\n\nretry_until with on_timeout = :false is the way to get a Bool instead of this, so a check can sit inside @test.\n\n\n\n\n\n","title":"Playwright.AssertionFailure"},{"category":"function","location":"api/#Base.fill","page":"API reference","text":"fill(loc::Locator, value; timeout=nothing)\n\nSet the matched input/textarea's value to value (extends Base.fill, so it needs no qualification even though it is not exported).\n\nThis clears the field first and fires an input event, which is what makes it different from assigning .value through evaluate — the page's listeners actually run.\n\nfill(locator(page, \"#name\"), \"Ada\")\nexpect(locator(page, \"#name\"); to_have_value = \"Ada\")\n\nElements that refuse to be filled — a range input, say — want evaluate plus dispatch_event instead.\n\n\n\n\n\n","title":"Base.fill"},{"category":"function","location":"api/#Base.count","page":"API reference","text":"count(loc::Locator) -> Int\n\nNumber of elements loc currently matches. This is a protocol round-trip, and the answer is a snapshot: a page that mutates the DOM can invalidate it immediately.\n\n\n\n\n\n","title":"Base.count"},{"category":"function","location":"api/#Base.length","page":"API reference","text":"length(stream::EventStream) -> Int\n\nHow many events are buffered right now, without consuming any. This is the one to poll on — pending_events drains, so it cannot be used to wait for a count to be reached.\n\ntimedwait(() -> length(stream) >= 3, 5.0)\n\n\n\n\n\n","title":"Base.length"},{"category":"function","location":"api/#Base.first","page":"API reference","text":"first(loc::Locator) -> Locator\n\nThe first match, as a single-element locator. Free — it is nth(loc, 1) and needs no round-trip to work that out.\n\nfirst(locator(page, \"li\"; strict = false))\n\n\n\n\n\n","title":"Base.first"},{"category":"function","location":"api/#Base.last","page":"API reference","text":"last(loc::Locator) -> Locator\n\nThe last match, as a single-element locator.\n\nUnlike Playwright's .last(), this costs a count round-trip to find out where the end is — the selector suffix it builds needs a concrete index.\n\nlast(locator(page, \"li\"; strict = false))\n\n\n\n\n\n","title":"Base.last"},{"category":"function","location":"api/#Base.iterate","page":"API reference","text":"iterate(loc::Locator)\n\nLocators are iterable, yielding one single-element Locator per match:\n\nfor slider in locator(page, \"input[type=range]\"; strict=false)\n    fill(slider, \"7\")\nend\n\nThe match set is sampled once, by a count call when iteration starts. A page that adds or removes matching elements mid-loop can leave later iterations resolving to different elements, or to none at all. That is inherent to Playwright's lazy-selector model rather than a defect here: a Locator is a query, not a snapshot of nodes.\n\nIterating a strict locator is allowed and simply yields at most one element — strictness is checked on action, and nth has already resolved the ambiguity.\n\n\n\n\n\n","title":"Base.iterate"},{"category":"function","location":"api/#Base.close","page":"API reference","text":"close(sub::Subscription)\nunsubscribe(sub::Subscription)\n\nDetach sub from the registry and empty its buffer, so buffered payloads become garbage immediately rather than at some later GC of the handle. Idempotent.\n\n\n\n\n\nclose(page::Page)\nclose(context::BrowserContext)\nclose(browser::Browser)\n\nClose a page, a context and all of its pages, or a browser and everything in it. Closing a page that new_page(browser) created also closes the context that was created to hold it.\n\n\n\n\n\n","title":"Base.close"},{"category":"type","location":"api/#Playwright.PlaywrightAPI","page":"API reference","text":"PlaywrightAPI\n\nRoot handle passed to the playwright block. Fields chromium and firefox are the launchable BrowserTypes; process is the driver subprocess and connection the protocol connection (internal).\n\n\n\n\n\n","title":"Playwright.PlaywrightAPI"},{"category":"constant","location":"api/#Playwright.DEFAULT_TIMEOUT","page":"API reference","text":"Package default for action timeouts, in milliseconds.\n\n\n\n\n\n","title":"Playwright.DEFAULT_TIMEOUT"},{"category":"function","location":"api/#Playwright.browsers_path","page":"API reference","text":"browsers_path() -> Union{String,Nothing}\n\nWhere browsers are installed to and looked up from, or nothing when the Playwright default cache is in use (~/.cache/ms-playwright on Linux, ~/Library/Caches/ms-playwright on macOS, %USERPROFILE%\\AppData\\Local\\ms-playwright on Windows).\n\nSet PLAYWRIGHT_BROWSERS_PATH to override it. The driver reads the variable itself, and both install and the driver process started by playwright inherit this process's environment, so setting it in ENV — or in the shell before starting Julia — is enough for installing and launching to agree on the location.\n\nWorth pointing at a project-local directory in CI, where a cache you control is easier to key and restore than one in the home directory.\n\n\n\n\n\n","title":"Playwright.browsers_path"},{"category":"section","location":"guide/errors/#Errors-and-timeouts","page":"Errors and timeouts","text":"Every failure surfaced from the driver is a PlaywrightError, so one catch covers the lot:\n\ntry\n    click(locator(page, \"#go\"))\ncatch e\n    e isa PlaywrightError || rethrow()\n    @warn \"click failed\" e.message\nend\n\nBut the useful thing is to branch on why it failed, without matching on message text:\n\nType Raised when\nTimeoutError an operation exceeded its timeout\nTargetClosedError the page, context or browser closed under the call\nAssertionFailure a retrying assertion never matched\nDriverError anything else, including JS exceptions\n\nAll four carry .message, .name and .stack. The classification comes from the name field the driver puts on its error replies, and an unrecognised name becomes a DriverError — so a future Playwright release cannot produce an error that escapes the taxonomy.","title":"Errors and timeouts"},{"category":"section","location":"guide/errors/#The-message-includes-the-call-log","page":"Errors and timeouts","text":"A timeout does not just say that it timed out. The driver's call log is appended to the message, the way upstream clients do it, so it names what the wait was waiting for:\n\nTimeoutError: Timeout 5000ms exceeded.\nCall log:\n  - waiting for locator(\"#submit\")\n  - locator resolved to <button disabled>…</button>\n  - element is not enabled\n\nThat last line is the answer: the selector was fine, the button was disabled.","title":"The message includes the call log"},{"category":"section","location":"guide/errors/#Which-error-means-what","page":"Errors and timeouts","text":"TimeoutError — something never happened in time. The page may be slow, or the selector may be wrong; the call log distinguishes them. Raising the timeout is occasionally right and usually a way of not reading the log.\n\nAssertionFailure — a retrying assertion never matched. Distinct from a bare timeout because it names both the expected and the received value:\n\nto_have_text failed on locator(\"h1\")\n  expected: \"Goodbye\"\n  received: \"Hello\"\n  (gave up after 5000ms of retrying)\n\nTargetClosedError — a lifecycle error rather than a failure of the call. The usual cause is a close in a finally racing work still in flight, or acting on a page whose context has already gone.\n\nDriverError — the catch-all, and what a JavaScript exception inside evaluate becomes:\n\ntry\n    evaluate(page, \"() => { throw new Error('boom') }\")\ncatch e\n    e isa DriverError && occursin(\"boom\", e.message)\nend","title":"Which error means what"},{"category":"section","location":"guide/errors/#Timeouts-cascade","page":"Errors and timeouts","text":"Every timeout keyword falls back through a chain: the call's own keyword, then the page's setting, then the context's, then 30 seconds.\n\nctx = new_context(browser)\nset_default_timeout!(ctx, 2_000)       # a missing element fails in 2 s\npage = new_page(ctx)\nset_default_timeout!(page, 5_000)      # ...but this page gets 5 s\n\nSetting it once on the context is the usual move. The 30-second default is generous for a passing suite and painful for a failing one — twenty missing elements is ten minutes of waiting for an answer you already know.\n\nset_default_navigation_timeout! does the same for navigations. Navigations fall back to the action setting when they have none of their own, so setting only the action default also shortens navigations rather than leaving them at 30 seconds.\n\nlaunch's own timeout is deliberately outside the cascade: it bounds browser startup, where there is no page or context to inherit from.","title":"Timeouts cascade"},{"category":"section","location":"guide/errors/#Postmortem-readers-do-not-raise","page":"Errors and timeouts","text":"console_messages and page_errors return an empty vector rather than raising once the page or context has closed.\n\nThey are postmortem readers, usually called from a finally while a more important error is already in flight. Throwing there would mask the failure you were trying to explain, and \"the page is gone\" tells a caller who is already handling an error nothing they can use.\n\nThe silence is bounded: that one error type, those readers only, and every other failure still propagates.","title":"Postmortem readers do not raise"},{"category":"section","location":"guide/errors/#A-note-on-numbers","page":"Errors and timeouts","text":"JavaScript has one number type, so evaluate returns Float64:\n\nevaluate(page, \"1 + 1\")        # 2.0\nevaluate(page, \"1 + 1\") == 2   # true\n\nNot an error, but it is the kind of thing that produces a confusing failure in a @test that compares types rather than values.","title":"A note on numbers"},{"category":"section","location":"guide/errors/#Changed-in-milestone-3","page":"Errors and timeouts","text":"PlaywrightError used to be a concrete struct and is now an abstract supertype. catch e isa PlaywrightError and e.message are unaffected; only PlaywrightError(msg) as a constructor breaks — use DriverError(msg). Construction was internal to this package.","title":"Changed in milestone 3"},{"category":"section","location":"examples/oxygen/#Oxygen.jl","page":"Oxygen.jl","text":"A micro-framework: routes plus a rendered page. What this adds over the HTTP.jl example is a second surface — a real app is a page plus the JSON endpoints that page calls, and both need testing.\n\n$ julia --project=examples examples/oxygen_jl.jl\n$ PLAYWRIGHT_JL_ENGINE=firefox julia --project=examples examples/oxygen_jl.jl","title":"Oxygen.jl"},{"category":"section","location":"examples/oxygen/#What-to-take-from-it","page":"Oxygen.jl","text":"The JSON is checked through the browser's own fetch, not through HTTP.jl on the Julia side. That is the point worth copying. Asserting from Julia tells you what Julia can reach; asserting through the page tells you what the page can reach — same origin, same cookies, same headers, same CORS rules. Those are different claims, and only one of them is the one your users depend on.\n\nitems = evaluate(page, \"() => fetch('/api/inventory').then(r => r.json())\")\n@test length(items) == 3\n\nevaluate awaits the promise and converts the body, so it is an ordinary Julia value by the time it reaches @test. The status code comes back the same way, which no DOM assertion could tell you.\n\nThe DOM starts empty and fills itself in from the route. Every DOM assertion on this page is therefore only true if the API works — the two surfaces are not independently checked, they are checked against each other.","title":"What to take from it"},{"category":"section","location":"examples/oxygen/#The-source","page":"Oxygen.jl","text":"Read from examples/oxygen_jl.jl at build time:\n\nusing Markdown, Playwright\nsrc = read(joinpath(pkgdir(Playwright), \"examples\", \"oxygen_jl.jl\"), String)\nMarkdown.parse(\"```julia\\n\" * src * \"```\")","title":"The source"},{"category":"section","location":"examples/http/#HTTP.jl","page":"HTTP.jl","text":"The thinnest possible Julia web server, driven end to end. This example is first on purpose: HTTP.jl brings no conventions of its own, so what is left on the page is the pattern every browser test follows rather than a framework's idioms.\n\n$ julia --project=examples examples/http_jl.jl\n$ PLAYWRIGHT_JL_ENGINE=firefox julia --project=examples examples/http_jl.jl","title":"HTTP.jl"},{"category":"section","location":"examples/http/#What-to-take-from-it","page":"HTTP.jl","text":"The lifecycle is a try/finally around everything that was started. The server is closed in one, the browser in another. A test that fails still tears down what it created, which matters more in CI than anywhere else — a leaked browser outlives the job.\n\nThe port is not hard-coded. HTTP.jl can pick one itself with listenany = true and hand it back, which is better than choosing a free port and then binding it: there is no window in between for something else to take it. The other examples use free_port() from common.jl only because their frameworks insist on being told a port up front.\n\nThe page mutates itself 300 milliseconds after the click, deliberately. That delay is handled by expect's retry and not by a sleep on the Julia side — which is the entire argument of Assertions in one line of test code.\n\nexpect to settle, @test to check. The assertions that wait are expect; once the DOM has settled, a plain read is the natural thing to put inside @test. Using each for its own job is what removes the temptation to sleep.","title":"What to take from it"},{"category":"section","location":"examples/http/#The-source","page":"HTTP.jl","text":"Read from examples/http_jl.jl at build time, so this page cannot drift from what CI actually ran:\n\nusing Markdown, Playwright\nsrc = read(joinpath(pkgdir(Playwright), \"examples\", \"http_jl.jl\"), String)\nMarkdown.parse(\"```julia\\n\" * src * \"```\")","title":"The source"},{"category":"section","location":"guide/locators/#Locators","page":"Locators","text":"A Locator is a query, not an element. It holds a frame, a selector string and a strictness flag, and nothing else — no reference to a node in the browser. Every action re-resolves the selector at the moment it runs.\n\nloc = locator(page, \"#name\")     # nothing has happened yet; no round trip\nfill(loc, \"Ada\")                 # now the selector is resolved, and acted on\n\nThat is the whole design, and everything below follows from it. A locator built before a re-render still works afterwards, because there was never a node to go stale.","title":"Locators"},{"category":"section","location":"guide/locators/#Selectors","page":"Locators","text":"The selector string is passed to Playwright, which understands more than CSS:\n\nForm Matches\n\"#name\", \".row td\" CSS, the default\n\"text=Submit\" by visible text\n\"xpath=//div[@role='main']\" XPath\n\"#outer >> .inner\" chained: .inner within #outer","title":"Selectors"},{"category":"section","location":"guide/locators/#Strictness","page":"Locators","text":"By default a locator is strict: acting on a selector that matches more than one element raises rather than silently picking the first.\n\nclick(locator(page, \"li\"))                    # raises if there are three <li>s\nclick(locator(page, \"li\"; strict = false))    # clicks the first\n\nStrictness is checked on action, not on construction, and it is a property of the locator rather than a setting on the page — so a suite that works with lists repeats strict = false. That is a known wart; see tasks/m5-api-gaps.md in the repository.","title":"Strictness"},{"category":"section","location":"guide/locators/#Several-matches","page":"Locators","text":"A locator that may match many is countable, indexable and iterable:\n\nrows = locator(page, \"tr\"; strict = false)\n\ncount(rows)             # 3 — a protocol round trip\nnth(rows, 2)            # the second row, 1-based; `rows[2]` is the same\nfirst(rows)             # free\nlast(rows)              # costs a `count` to find the end\n\nfor row in rows\n    println(text_content(row))\nend\n\nEach of these gives back a single-element locator, which is strict by construction — nth has already resolved the ambiguity that strictness exists to catch.\n\nwarning: A `Locator` is deliberately not an `AbstractArray`\nIndexing is a network call and the length is not stable between two calls. Claiming the array interface would promise more than a selector can deliver. Iteration samples the match set once, with a count round trip, when the loop starts — a page that adds or removes matching elements mid-loop can leave later iterations resolving elsewhere, or nowhere.","title":"Several matches"},{"category":"section","location":"guide/locators/#Reading","page":"Locators","text":"Call Returns\ntext_content textContent — raw, including hidden nodes\ninner_text innerText — rendered, hidden text excluded\ninner_html innerHTML — markup\ninput_value an input's live value\nget_attribute one attribute, or nothing\nis_visible, is_checked, is_enabled state\n\nEvery one of these reads once and returns. None of them waits. That makes them the wrong tool for \"has it appeared yet\", and the right tool for \"what is there now, having already established that it is settled\".\n\n# Wrong: reads too early, fails, and the failure looks like a bug in the page\n@test text_content(locator(page, \"#status\")) == \"ready\"\n\n# Right: waits for it, then the read is safe\nexpect(locator(page, \"#status\"); to_have_text = \"ready\")\n\nSee Assertions.","title":"Reading"},{"category":"section","location":"guide/locators/#Acting","page":"Locators","text":"click(locator(page, \"#greet\"))\nfill(locator(page, \"#name\"), \"Ada\")\n\nclick waits for the element to be actionable first: attached, visible, stable, able to receive events, and not disabled. That wait is why a click on a button that is enabled a moment later needs no sleep in front of it.\n\nfill clears the field and fires an input event, which is what makes it different from assigning .value through evaluate — the page's listeners actually run.\n\nFor controls ordinary interaction cannot drive exactly — a range input, say — combine evaluate with dispatch_event:\n\nslider = locator(page, \"#volume\")\nevaluate(slider, \"(el, v) => el.value = v\", 7)\ndispatch_event(slider, \"input\")            # let the listeners react","title":"Acting"},{"category":"section","location":"guide/locators/#Locators-inside-iframes","page":"Locators","text":"An iframe is a separate document, and a page-level locator cannot see into one. Scope in with frame_locator:\n\ninner = frame_locator(page, \"iframe#checkout\")\nclick(locator(inner, \"button[type=submit]\"))\n\nframe_locator chains, for an iframe inside an iframe. The two directions between an element and a frame are content_frame — the frame an <iframe> element contains — and owner_frame — the frame an element lives in.\n\nTo inspect the frame tree rather than act inside it, use frames, parent_frame, url and name.","title":"Locators inside iframes"},{"category":"section","location":"guide/locators/#Handles,-and-when-to-want-one","page":"Locators","text":"element_handle resolves a locator now and gives back an ElementHandle — a real reference to one element, which goes stale when the page re-renders.\n\nPrefer the locator. The one good reason to want a handle is to pass an element into JavaScript as an argument:\n\nhandle = element_handle(locator(page, \"#chart\"))\ntry\n    evaluate(page, \"el => el.getBoundingClientRect().width\", handle)\nfinally\n    dispose(handle)\nend\n\nelement_handle does not wait; wait_for_selector is the form that does. See Waiting.","title":"Handles, and when to want one"},{"category":"section","location":"guide/locators/#Reading-a-locator-back","page":"Locators","text":"frame, selector and is_strict return the three things a locator holds, which is occasionally useful when writing a helper that takes one.\n\nselector(nth(locator(page, \"li\"; strict = false), 3))   # \"li >> nth=2\"","title":"Reading a locator back"},{"category":"section","location":"guide/waiting/#Waiting","page":"Waiting","text":"Browsers are asynchronous and pages are slow in ways that vary run to run. The whole difficulty of browser testing is deciding when to look. There are two answers, and only one of them works.\n\nsleep(2)                                             # hope\nwait_for_selector(page, \"#late\"; state = :visible)    # know\n\nA sleep is wrong in both directions at once: too short on a loaded CI runner, and wasted on every run where the page was fast. The cost compounds — a suite of two hundred tests with a two-second sleep each spends seven minutes doing nothing.","title":"Waiting"},{"category":"section","location":"guide/waiting/#Wait-driver-side","page":"Waiting","text":"Everything in this package waits in the browser, not in Julia. The condition is re-checked by the driver, so an element that arrives late is picked up the moment it arrives rather than at the next poll.\n\nCall Waits for\nexpect an assertion to hold — see Assertions\nwait_for_selector an element to reach a DOM state\nwait_for_function a JavaScript predicate to go truthy\nretry_until anything else, polled from Julia\nclick and the other actions the element to be actionable\n\nActions waiting on their own is the part that is easy to miss: click already waits for its target to be attached, visible, stable, able to receive events and not disabled. Most of the time no explicit wait is needed at all.","title":"Wait driver-side"},{"category":"section","location":"guide/waiting/#wait_for_selector","page":"Waiting","text":"wait_for_selector(page, \"#late\"; state = :visible)\nwait_for_selector(page, \"#spinner\"; state = :hidden)\n\nstate is one of :attached, :detached, :visible or :hidden. The default is :visible, which is what you want the great majority of the time — an element present in the DOM but not shown is not something a user can use.\n\nIt returns an ElementHandle (or nothing for :detached and :hidden), and raises TimeoutError if the state never arrives. It works on a Page, a Frame or a Locator.","title":"wait_for_selector"},{"category":"section","location":"guide/waiting/#wait_for_function","page":"Waiting","text":"For a condition that is about the page rather than about one element:\n\nwait_for_function(page, \"() => window.appReady === true\")\nwait_for_function(page, \"n => document.querySelectorAll('li').length >= n\", 3)\n\nThe expression is evaluated in the browser until it returns something truthy. polling chooses between \"raf\" (every animation frame, the default) and a number of milliseconds.\n\nThis is the tool for application-level readiness — a framework that sets a flag when it has hydrated, say. It sees the page's own state, which no DOM selector can.","title":"wait_for_function"},{"category":"section","location":"guide/waiting/#retry_until","page":"Waiting","text":"The escape hatch, and the only one of these that polls from the Julia side. Use it when the condition is not about the page at all.\n\nretry_until(; timeout = 60_000, interval = 200, on_error = :retry) do\n    HTTP.get(url).status == 200\nend\n\nTwo keywords carry the weight:\n\nKeyword Values Meaning\non_timeout :throw (default), :false raise AssertionFailure, or return false\non_error :throw (default), :retry propagate a predicate exception, or treat it as \"not yet\"\n\non_error = :retry is the one that makes waiting for a server possible. While a web framework is starting, a request does not return a bad status — it throws connection refused. Treating that exception as \"not yet\" is the difference between a wait that works and a wait that crashes on its first attempt:\n\n# examples/common.jl, near enough\nfunction wait_for_server(url; timeout = 60_000)\n    ok = retry_until(; timeout, interval = 200, on_error = :retry, on_timeout = :false) do\n        HTTP.get(url; retry = false, status_exception = true)\n        true\n    end\n    ok || error(\"server at $url did not come up within $(timeout)ms\")\nend\n\nThe Genie.jl example measures what this is worth: 25 seconds between up() returning and the first request being answered, on an idle workstation. A sleep long enough to cover that is a sleep paid on every other example forever. WGLMakie.jl does the same for rendering, where a cold first paint took 57.9 seconds.\n\non_timeout = :false is what lets a check sit inside @test and report a Test.Fail rather than an Error, so the testset summary reads correctly:\n\n@test retry_until(page; on_timeout = :false) do\n    length(console_messages(page)) >= 3\nend\n\nPrefer expect whenever it fits. retry_until round-trips per attempt and can miss a state that flickers between polls; expect re-checks inside the browser and cannot.","title":"retry_until"},{"category":"section","location":"guide/waiting/#Timeouts-cascade","page":"Waiting","text":"Every timeout keyword falls back through a chain: the call's own keyword, then the page's setting, then the context's, then 30 seconds.\n\nctx = new_context(browser)\nset_default_timeout!(ctx, 2_000)       # a missing element fails in 2 s\npage = new_page(ctx)\nset_default_timeout!(page, 5_000)      # ...but this page gets 5 s\n\nSetting it once on the context is usually right: the default of 30 seconds per miss is generous for a passing suite and painful for a failing one.\n\nset_default_navigation_timeout! does the same for navigations, which fall back to the action setting when they have none of their own — so setting only the action default also shortens navigations rather than leaving them at 30 seconds.\n\nlaunch's own timeout is deliberately outside the cascade: it bounds browser startup, where there is no page or context to inherit from.\n\nSee Errors and timeouts for what happens when the budget runs out.","title":"Timeouts cascade"},{"category":"section","location":"guide/assertions/#Assertions","page":"Assertions","text":"expect is the assertion you should reach for by default. It retries the condition in the browser until it holds or the timeout runs out, and returns its target so calls chain.\n\nexpect(locator(page, \"h1\"); to_have_text = \"Hello\")\n\nThe difference from an ordinary @test is not stylistic. @test text_content(loc) == \"Hello\" reads the DOM once, at whatever moment the previous line happened to finish, and fails if the page was a few milliseconds behind. The same assertion written with expect passes as soon as the text arrives, and only fails if it never does.","title":"Assertions"},{"category":"section","location":"guide/assertions/#Matchers","page":"Assertions","text":"Pass one or more as keyword arguments:\n\nexpect(locator(page, \"h1\");    to_have_text = \"Hello\")\nexpect(locator(page, \"li\"; strict = false); to_have_count = 3)\nexpect(locator(page, \"#box\");  to_have_value = r\"^se\")\nexpect(locator(page, \"#link\"); to_have_attribute = \"href\" => \"/somewhere\")\nexpect(locator(page, \"#gone\"); to_be_visible = false)\n\nMatcher Expects\nto_have_text exact text — a String or a Regex\nto_contain_text text containing the given substring or match\nto_have_value an input's value\nto_have_count how many elements the locator matches\nto_have_attribute name => value\nto_be_visible, to_be_hidden true/false\nto_be_enabled, to_be_disabled true/false\nto_be_checked true/false\n\nSeveral matchers in one call are checked one after another and the first failure raises. timeout applies to each matcher, since each is its own retrying check.","title":"Matchers"},{"category":"section","location":"guide/assertions/#Negation","page":"Assertions","text":"Wrap any expectation in Not:\n\nexpect(locator(page, \"h1\"); to_have_text = Not(\"Goodbye\"))\n\nFor the boolean matchers, = false says the same thing more readably — to_be_visible = false rather than to_be_visible = Not(true).","title":"Negation"},{"category":"section","location":"guide/assertions/#Asserting-about-the-document","page":"Assertions","text":"Hand expect a Page or a Frame and it asserts about the document instead:\n\nexpect(page; to_have_title = \"Checkout\")\nexpect(page; to_have_url = r\"/checkout\\$\")\n\nMatchers are partitioned by target, so to_have_text on a Page — or to_have_title on a Locator — raises an ArgumentError naming the matcher that does work. That is worth more than it sounds: a driver-side failure from a mismatched matcher looks exactly like a real assertion failure, and would cost an afternoon.","title":"Asserting about the document"},{"category":"section","location":"guide/assertions/#Failures-name-both-values","page":"Assertions","text":"to_have_text failed on locator(\"h1\")\n  expected: \"Goodbye\"\n  received: \"Hello\"\n  (gave up after 5000ms of retrying)\n\nThe received value is in the message, so a red CI run does not need to be reproduced locally to find out what was actually on the page. The exception type is AssertionFailure.","title":"Failures name both values"},{"category":"section","location":"guide/assertions/#Inside-a-@testset","page":"Assertions","text":"expect raises rather than returning a Bool, which is what makes retrying possible — there is no value to return until the condition settles. Inside a testset it therefore registers as an Error rather than a Fail.\n\nThat is usually fine, and the message is the same either way. When the distinction matters — a summary that should read \"1 failed\" rather than \"1 errored\" — retry_until with on_timeout = :false gives you a Bool to put inside @test:\n\n@test retry_until(page; on_timeout = :false) do\n    length(console_messages(page)) >= 3\nend","title":"Inside a @testset"},{"category":"section","location":"guide/assertions/#When-expect-does-not-fit","page":"Assertions","text":"retry_until is the escape hatch for conditions no matcher expresses — see Waiting, which covers it in full. Prefer expect where it fits: it re-checks inside the browser, so it neither round-trips per attempt nor misses a state that flickers between polls.","title":"When expect does not fit"},{"category":"section","location":"guide/assertions/#A-worked-example","page":"Assertions","text":"From examples/http_jl.jl, which asserts about a page that mutates itself 300 milliseconds after a click:\n\n# Nothing has been greeted yet. `to_have_count = 0` is a real assertion about\n# absence, not a missing element swallowed.\nexpect(locator(page, \"li.greeting\"; strict = false); to_have_count = 0)\n\nfill(locator(page, \"#name\"), \"Ada\")\nexpect(locator(page, \"#name\"); to_have_value = \"Ada\")\nclick(locator(page, \"#greet\"))\n\n# The 300ms delay is handled by expect's retry, not by a sleep on this side.\nexpect(locator(page, \"li.greeting\"); to_have_text = \"Hello, Ada!\")\nexpect(locator(page, \"li.greeting\"; strict = false); to_have_count = 1)\n\n# Once it has settled, a plain read is the natural thing to `@test`.\n@test text_content(locator(page, \"li.greeting\")) == \"Hello, Ada!\"\n\nThe last two lines are the pattern worth taking away: expect to settle, @test to check. Reads never wait, assertions always do, and using each for its own job removes the temptation to sleep.","title":"A worked example"},{"category":"section","location":"#Playwright.jl","page":"Home","text":"Drive real browsers — Chromium and Firefox — from Julia, through the official Playwright automation engine.\n\nusing Playwright\n\nplaywright() do pw\n    browser = launch(pw.chromium; headless = true)\n    try\n        page = new_page(browser)\n        goto(page, \"https://example.com\")\n        expect(locator(page, \"h1\"); to_have_text = \"Example Domain\")\n    finally\n        close(browser)\n    end\nend","title":"Playwright.jl"},{"category":"section","location":"#What-it-is","page":"Home","text":"Playwright.jl speaks Playwright's own JSON protocol to the same driver the Node, Python and .NET clients use. There is no browser code in this package and no scraping of anyone's internals: the driver is the upstream playwright-core npm package, run by a pinned Node binary, both downloaded and assembled on first use.\n\nThat has one consequence worth stating up front, because it decides whether this package suits you: the first run downloads a driver and a browser. See Getting started for how to do that once, in CI, rather than on every run.","title":"What it is"},{"category":"section","location":"#What-it-is-for","page":"Home","text":"Testing Julia web applications in a real browser. The examples drive HTTP.jl, Oxygen.jl, Genie.jl and WGLMakie.jl — a plain server, a micro-framework, a full framework, and a WebGL app — and each one is a script you can copy.\n\nThe design leans hard on one idea, inherited from Playwright: waiting is not sleeping. Every assertion retries until it holds or its timeout runs out, so a test does not need to know how long the page will take.\n\n# Not this\nsleep(2)\n@test text_content(locator(page, \"#status\")) == \"ready\"\n\n# This\nexpect(locator(page, \"#status\"); to_have_text = \"ready\")","title":"What it is for"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"If you want to Read\ninstall it and write a first test Getting started\nfind elements Locators\nstop using sleep Waiting and Assertions\ncatch popups, console output, page errors Events\nget a screenshot or a trace out of a failing test Artifacts\nknow which error means what Errors and timeouts\nlook a function up API reference","title":"Where to go next"},{"category":"section","location":"#Status","page":"Home","text":"Milestones 1–5 are complete: the protocol layer, locators and evaluation, waiting and assertions, artifacts and the failure path, and this documentation. The package is not registered yet.\n\nChromium and Firefox are supported on Linux. WebKit is not tested.","title":"Status"},{"category":"section","location":"examples/genie/#Genie.jl","page":"Genie.jl","text":"The full framework, and the slowest to start — which is exactly why it is here.\n\n$ julia --project=examples examples/genie_jl.jl\n$ PLAYWRIGHT_JL_ENGINE=firefox julia --project=examples examples/genie_jl.jl","title":"Genie.jl"},{"category":"section","location":"examples/genie/#The-number-this-example-exists-for","page":"Genie.jl","text":"Genie answered its first request 25.0 seconds after up() returned, on an idle workstation with a warm depot. The browser work that follows takes two seconds. A cold CI runner is slower.\n\nThat gap is the whole argument for retry_until(…; on_error = :retry). During warm-up a request does not return a bad status — it throws connection refused. Treating that exception as \"not yet\" rather than as a failure is the difference between a wait that works and one that crashes on its first attempt:\n\nretry_until(; timeout, interval = 200, on_error = :retry, on_timeout = :false) do\n    HTTP.get(url; retry = false, status_exception = true)\n    true\nend\n\nA sleep long enough to cover 25 seconds is 25 seconds paid on every run forever, including the runs where Genie was ready in five. A shorter one is a flake waiting for a loaded runner. The example measures and prints the wait on every run, so the claim above stays checkable rather than becoming folklore.\n\nSee Waiting.","title":"The number this example exists for"},{"category":"section","location":"examples/genie/#What-else-to-take-from-it","page":"Genie.jl","text":"Routing is the thing under test. The example follows a link, asserts on the URL it landed on and on the rendered article, then comes back — because routing is what Genie brings that the previous two examples do not.","title":"What else to take from it"},{"category":"section","location":"examples/genie/#The-source","page":"Genie.jl","text":"Read from examples/genie_jl.jl at build time:\n\nusing Markdown, Playwright\nsrc = read(joinpath(pkgdir(Playwright), \"examples\", \"genie_jl.jl\"), String)\nMarkdown.parse(\"```julia\\n\" * src * \"```\")","title":"The source"},{"category":"section","location":"guide/artifacts/#Artifacts","page":"Artifacts","text":"What to reach for when a suite goes red in CI and the assertion message is not enough.\n\nThe problem this solves is specific: the run that failed happened on a machine you cannot see, in a browser that no longer exists, and it will probably pass when you run it again. Evidence has to be collected at the time, by the failing run itself.\n\nCall Produces\nwith_page all of the below, on failure\nwith_tracing a trace zip, written however the block exits\nstart_tracing / stop_tracing the same, unpaired\nscreenshot PNG bytes, and a file if you pass path\nvideo a .webm per page, with record_video on the context\npdf PDF bytes — Chromium only\nreport_diagnostics screenshot.png, console.log, errors.log","title":"Artifacts"},{"category":"section","location":"guide/artifacts/#The-fixture","page":"Artifacts","text":"with_page is the per-test form, and the one to start with:\n\nwith_page(browser, url; artifacts = \"artifacts/checkout\") do page\n    expect(page; to_have_title = \"Checkout\")\n    click(locator(page, \"#submit\"))\n    expect(locator(page, \"#receipt\"); to_be_visible = true)\nend\n\nIt opens a page, navigates, always closes it, and — when the body throws — dumps diagnostics before closing, because afterwards there is nothing left to look at.\n\nYour exception propagates unchanged. A failure while collecting the evidence is a @warn, never a replacement for the failure being diagnosed. A helper that adds evidence must not destroy the thing it was called to explain.\n\nartifacts_on = :always captures on success too; the default :failure keeps a large suite from writing a screenshot per passing test.","title":"The fixture"},{"category":"section","location":"guide/artifacts/#Tracing","page":"Artifacts","text":"A trace is the full record of what the browser did — actions, DOM snapshots before and after each one, console output, network — viewable afterwards in Playwright's own viewer.\n\nwith_tracing(ctx; path = \"artifacts/trace.zip\", screenshots = true) do\n    goto(page, url)\n    click(locator(page, \"#submit\"))     # if this throws, the zip is still written\nend\n\n\"However the block exits\" is the point: the run worth tracing is the one that threw.\n\nOpen the result with the upstream viewer. This package neither builds nor parses a trace — the zip is assembled by the driver and is an opaque artifact for the viewer:\n\n$ npx playwright@1.61.1 show-trace artifacts/trace.zip\n\nThe unpaired form is start_tracing and stop_tracing. Each stop consumes the chunk the start opened, so tracing a second run means starting again.\n\nnote: `sources = true` is not available\nUpstream clients embed calling source files by passing includeSources when they assemble the zip themselves. This package lets the driver assemble it, and the driver's tracingStart carries no sources flag. Passing sources = true raises rather than being silently dropped — a flag that quietly does nothing is worse than one that is not offered.","title":"Tracing"},{"category":"section","location":"guide/artifacts/#Video","page":"Artifacts","text":"Video is recorded per page, and switched on at the context:\n\nctx = new_context(browser; record_video = (dir = \"artifacts/video\",))\npage = new_page(ctx)\ngoto(page, url)\n\nwarning: The video does not exist until the page closes\nThe driver knows the eventual path immediately, but the file is not finished until its page or context closes. path is what waits:recording = video(page)\nclose(page)\n@test isfile(path(recording))","title":"Video"},{"category":"section","location":"guide/artifacts/#Screenshots-and-PDFs","page":"Artifacts","text":"screenshot returns the PNG bytes and writes them to path if you give one, so a screenshot can be attached to a report without touching the filesystem:\n\nscreenshot(page; path = \"artifacts/checkout.png\")\nbytes = screenshot(page)\n\npdf is the same shape, and is Chromium only — off Chromium it raises an ArgumentError naming the engine, decided client-side with no round trip.","title":"Screenshots and PDFs"},{"category":"section","location":"guide/artifacts/#Diagnostics","page":"Artifacts","text":"report_diagnostics writes the three things you want at once — screenshot.png, console.log and errors.log:\n\ntry\n    click(locator(page, \"#submit\"))\ncatch\n    report_diagnostics(page, \"artifacts/failure\")\n    rethrow()\nend\n\nIt is what with_page calls for you.","title":"Diagnostics"},{"category":"section","location":"guide/artifacts/#The-Artifact-type","page":"Artifacts","text":"Traces and videos come back as an Artifact — a file the driver is producing. Three verbs:\n\nCall Does\npath blocks until it is completely written, returns where it is\nsave_as copies it somewhere of your choosing, creating directories\ndelete removes the driver's copy\n\nrecording = video(page)\nclose(page)\nif test_passed\n    delete(recording)                          # nothing to look at\nelse\n    save_as(recording, \"artifacts/run.webm\")   # your copy; delete cannot touch it\nend\n\nThe driver writes these into a temporary directory that lives as long as the browser, so a passing test that keeps everything is a slow leak.","title":"The Artifact type"},{"category":"section","location":"guide/artifacts/#Keep-them-out-of-git","page":"Artifacts","text":"Traces, videos, screenshots and PDFs are binaries, regenerated on every run. Add the directory you write them to your .gitignore — this repository ignores artifacts/.","title":"Keep them out of git"}]
}
